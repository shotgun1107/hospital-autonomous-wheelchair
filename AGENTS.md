# AGENTS.md

## 프로젝트 목표와 현재 단계

병원 관계자의 예약 이동과 환자의 직접 호출을 한 대의 축소형 자율주행 휠체어 운행으로 연결하는 종단 시스템 POC를 기획한다.

- 실제 사람은 태우지 않고 인형만 사용한다.
- 알려진 지도를 사용하는 통제된 단층 시연 공간을 전제로 한다.
- 현재는 기획·분석 단계이며 제품 구현 단계가 아니다.
- 개인 승인과 팀 전체 합의를 구분한다.

## 대화와 문서

- 사용자와의 기본 대화 및 프로젝트 문서는 한국어로 작성한다.
- 코드 식별자, 프로토콜 필드와 파일명은 영문 `snake_case`를 기본으로 한다.
- 확정 사실, 사용자 승인, 팀 합의, 가설과 브레인스토밍을 구분한다.
- 불확실한 임상·규제·안전 사항을 단정하지 않는다.
- 큰 설계 결정은 `docs/decisions/`에 ADR로 남긴다.
- 코드와 문서 또는 문서끼리 충돌하면 임의로 한쪽을 정답으로 고치지 말고 충돌 위치와 영향을 먼저 보고한다.

## 기준 문서와 우선순위

현재 제품 기획의 최우선 기준은 다음 문서다.

1. `README.md`
2. `docs/product/big-picture.md`
3. `docs/product/path-planning-baseline.md`
4. `docs/product/path-planning-functional-scenarios.md`
5. `docs/product/path-planning-dependencies.md`
6. `docs/product/path-planning-strategy-comparison.md`
7. `docs/safety/path-safety-authority-flow.md`
8. `docs/product/path-planning-conditional-recommendation.md`
9. `docs/product/open-questions.md`

추가 참고 문서는 다음과 같다.

- 제품 개념: `docs/product/concept.md`
- 제품 범위: `docs/product/scope.md`
- 사용자·운용 시나리오: `docs/product/users.md`, `docs/product/scenarios.md`
- 요구사항 후보: `docs/product/requirements.md`
- 안전 개념: `docs/safety/safety-concept.md`
- 시스템 책임 경계: `docs/architecture/authority-boundaries.md`
- 시스템 개요: `docs/architecture/system-overview.md`
- 대회 MVP와 일정 초안: `docs/competition/mvp-and-schedule.md`

세부 참고 문서가 위 제품 기획 기준선과 충돌하면, 팀 합의 전 세부 문서는 구현 지시로 사용하지 않는다.

## 현재 승인 상태와 작업 금지선

- 경로 분석 계획의 1~6단계는 사용자 승인 기준으로 완료됐다.
- 이 승인은 팀 전체 합의나 구현 채택을 의미하지 않는다.
- `G1~G5`는 팀에서 확인되지 않았다.
- 전체 경로 재선택, 제한적 현재 경로 수정과 두 기능의 조합 운용 정책은 모두 `추천 보류`다.
- 사용자가 명시적으로 시작시키기 전에는 7단계 팀 결정 게이트를 진행하지 않는다.
- 7단계 전에는 최종 경로 전략이나 구체 알고리즘을 채택하지 않는다.
- 제품 코드 작성, 센서·알고리즘·수치·인터페이스 확정과 8단계 상세 설계를 시작하지 않는다.

## 역할과 책임

역할 분배는 완료됐으므로 다시 역할 분배를 질문하지 않는다.

### Software A

- 사용자는 Software A 담당이다.
- 경로 기능 분석, 경로 계획과 자율주행 소프트웨어 영역을 담당한다.
- 현재 단계에서는 후보 비교, 추상 입출력, 실패 조건과 검증 계획까지만 독자적으로 진행할 수 있다.
- 차체·센서·실행 경계의 실제 값을 임의로 확정하지 않는다.

### 소프트웨어 팀 범위

- 서버
- 프론트엔드
- ROS 2
- 자율주행 알고리즘
- AI
- 시뮬레이터
- 하드웨어 통신 게이트웨이
- 하드웨어 연동 프로토콜의 설계·구현·검증

소프트웨어 영역끼리 내부 구현을 직접 참조하지 말고 명시적인 계약을 통해 연결한다.

### 하드웨어·펌웨어·임베디드 팀 범위

- 회로, 배선, PCB와 전원
- 차체와 기구 설계
- 센서·모터 드라이버 선정
- MCU 펌웨어와 저수준 모터 제어
- 물리 E-stop, 범퍼, watchdog과 최종 구동 차단의 실제 집행

소프트웨어 팀은 외부 팀 산출물을 대신 구현하지 않는다. 연동에 필요한 요구사항, 프로토콜, 오류 상태, 시험 벡터와 관측 결과를 관리한다.

## 안전 불변조건

다음 조건을 깨는 설계와 구현은 허용하지 않는다.

1. 실제 사람을 축소 POC에 태우지 않는다.
2. 충돌 가능성이 생기거나 필수 판단이 불충분하면 경로 변경보다 로봇 로컬 안전정지를 먼저 개시한다.
3. 정지 요청·정지 동작과 실제 정지 완료를 같은 상태로 간주하지 않는다.
4. 실제 정지 완료 전에는 원래 경로 재개나 대체 경로를 실행하지 않는다.
5. 경로 기능의 `재개 가능` 또는 대체 경로 후보를 실제 이동 허가로 간주하지 않는다.
6. 물리 안전 금지와 로봇 로컬 안전 거부는 서버·경로 기능·사용자 요청보다 우선한다.
7. 서버 응답이 없어도 로봇 로컬 영역은 안전정지를 개시할 수 있어야 한다.
8. 위험 해소, 통신 복구, 입력 복구 또는 비상정지 해제만으로 자동 재출발하지 않는다.
9. 현재 미션·지도·위치·장애물·정지 상태와 일치하지 않거나 최신성을 판단할 수 없는 경로 결과를 실행하지 않는다.
10. 승인되지 않은 목적지·경로·구역으로 이동하지 않는다.
11. QR이 현재 미션과 일치하지 않으면 목적지 운행을 시작하지 않는다.
12. 승객의 정지·도움 요청은 즉시 정지를 개시하며, 도움 요청 뒤에는 지정된 담당 의료진 확인과 새로 생성되거나 명시적으로 재유효화된 재개 지시 전까지 이동하지 않는다.
13. 새 미션 전 배터리 운행 가능 여부를 확인하고, 임계 전원 문제에서는 안전정지를 우선한다.
14. 소프트웨어·시뮬레이션·축소 실물 시험 결과를 실제 사람 탑승 안전성이나 의료기기 인증의 증거로 확대하지 않는다.

## 변경과 검증

- 현재 기획 단계에서는 문서 읽기·충돌 보고·조건부 비교를 구현보다 우선한다.
- 새 기능은 팀 합의 뒤 요구사항과 시험 근거를 연결한다.
- 알고리즘 평가는 성공 사례뿐 아니라 실패 입력, 보수적 정지와 로그를 함께 보존한다.
- Python 논리 시험, Unity 시각 시험, 축소 실물 시험과 종단 통합 시험의 증거 범위를 구분한다.
- 실제 사람 탑승, 모터 출력 증가 또는 안전 제한 완화는 사용자 확인 없이 수행하지 않는다.
- 생성 로그와 대용량 결과물은 기본적으로 Git에 커밋하지 않는다.

### 임시 연구 하네스 실행 규칙

`simulation/` 아래의 비교 실험실과 일회성 연구 하네스에는 다음 순서를 적용한다.

1. 실행 전에 기존 로그·수집 시험 수·후보 수로 예상 시간을 먼저 계산하고 사용자에게 짧게 알린다.
2. `정적 검사·표적 단위시험 → 대표 public/golden 사례 → 읽기 전용 최종 감사 → 공개 episode 병렬 실행 → 전체 회귀` 순서로 진행한다.
3. 최종 감사와 코드 동결 전에 장시간 전체 회귀를 시작하지 않는다. 전체 회귀 뒤 코드를 바꿨다면 영향권 시험을 먼저 실행하고 마지막 전체 회귀는 한 번만 다시 수행한다.
4. 서로 독립적인 episode·seed·시험 파일의 기능·안전 결과 계산은 process 기반으로 병렬화한다. paired 조건, seed, corpus, 후보 수, tick 수, 안전 기준과 판정 기준은 병렬화를 이유로 바꾸거나 줄이지 않는다.
5. wall-clock 실행시간 자체가 판정 근거인 timing benchmark는 CPU contention이 없는 상태에서 직렬 실행한다. 기능·안전 병렬 결과를 timing 자격 근거로 사용하지 않는다.
6. worker 수는 실제 CPU와 메모리를 확인해 정한다. 기능·안전 batch에서는 과도한 유휴를 피하고 대략 절반 수준의 CPU 활용을 운영 목표로 삼되, 시스템 불안정이나 메모리 압박이 생기면 낮춘다. CPU 사용률 자체를 시험 합격 기준으로 삼지 않는다.
7. 이미 장시간이 걸리는 것으로 확인된 전체 suite를 하나의 직렬 명령으로 다시 실행하지 않는다. 파일·episode를 누락 없이 분할하고 각 worker에 고유한 임시 output 경로를 준다.
8. timeout 또는 중단 뒤 동일한 장시간 명령을 그대로 재실행하지 않는다. 병목과 마지막 완료 지점을 확인한 뒤 분할·병렬화·캐시 재사용 중 의미를 바꾸지 않는 방법을 먼저 적용한다.
9. 중단된 partial 산출물은 삭제하거나 최종 근거로 승격하지 않는다. 새 실행은 별도 경로에 저장하고 완주한 결과만 보고한다.
10. 임시 하네스의 통과는 offline·simulation 연구 증거일 뿐이며 online controller 동작, 제품 알고리즘 채택 또는 실제 사람 안전 증거로 확대하지 않는다.

## 현재 독자적으로 가능한 작업

- 승인된 문서의 정합성·충돌·누락 분석
- 미확정 의존성을 변수로 둔 경로 후보 비교
- 추상 시나리오, 기능 입출력, 실패 조건과 보수적 종료 정의
- 논리·시각·축소 실물·종단 시험의 역할 구분
- 팀 확인용 `G1~G5` 질문과 의사결정 자료 준비

`G1~G5`에 실제 답을 대입해 후보를 채택하거나 7단계 결정을 내리는 작업은 사용자의 명시적 시작 지시를 기다린다.
