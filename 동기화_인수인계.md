# 회사 PC → 집 PC 동기화 인수인계

> 작성 시각: 2026-08-14 (Asia/Seoul)
>
> 저장소: `https://github.com/shotgun1107/hospital-autonomous-wheelchair.git`
>
> 정본 브랜치: `main`
>
> 최신 구현 기준 커밋: `c66fe1d932b37a9b5afbc4a171195928afcce295`
>
> 기준 tree: `8eee0570929761ca62c8409bef3f22fe23799200`

이 문서는 회사 PC의 2026-08-14 작업을 종료하고 집 PC에서 R5를 이어가기 위한
인수인계다. 제품 알고리즘 채택, `G1~G5`, 경로 분석 7단계 팀 결정이 아니다.

---

## 1. 종료 시점 Git 상태

2026-08-14 회사 PC에서 직접 확인한 상태다.

```text
branch                   main
HEAD                     c66fe1d932b37a9b5afbc4a171195928afcce295
HEAD tree                8eee0570929761ca62c8409bef3f22fe23799200
origin/main              c66fe1d932b37a9b5afbc4a171195928afcce295
tracked working tree     clean
staged                    none
untracked                none
stash                     none
origin                    https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
```

이 문서를 커밋하면 최종 `main` HEAD는 위 구현 기준점보다 한 커밋 이상 앞선다.
집 PC에서는 본문의 구현 기준 커밋을 ancestor로 확인하고, 실제 최종 HEAD는
`git ls-remote origin refs/heads/main`과 `git rev-parse HEAD` 일치로 확인한다.

### 회사 PC에만 남은 선택 사항

```text
branch: codex/legacy-python-dwa-experiment-20260812
commit: 0f9654e chore: preserve experimental Python DWA investigation
upstream: none
```

이 브랜치는 현재 R5 `main`의 필수 의존성이 아니다. 이번 동기화에서 원격 push·merge·삭제하지
않고 회사 PC에 보존했다. 집에서 해당 과거 실험을 꼭 봐야 하는 경우에만 별도 bundle 또는
선택적 원격 브랜치로 이전한다.

---

## 2. 현재 연구 단계

### 완료한 기반

- R1: 기존 합성 지도·경로 알고리즘 실험실과 기초 회귀 시험
- R2: ground-truth witness 자동화·감사를 수행했으나, 최종 공개 감사는 `FAIL`이며
  R2 완료 자격은 미충족
- R3: Actor를 제거한 정적 bounded spatial oracle 공개 `21/21 PASS`
- R4: R3 경로를 immutable full reference·sliding local window로 변환한 공개 `21/21 PASS`
- R5-1: persistent controller input/result/reference-binding 계약
- R5-2: RPP·DWB 공통 section executor
- R5-3: persistent RPP adapter
- R5-4: source-derived persistent DWB adapter
- R5-5: reference-bound proposal/context와 shared safety gate를 연결한 20 Hz functional pipeline

### R5-5에서 직접 확인한 것

- `wide-straight-left` RPP 60-tick prefix와 전체 terminal completion이 external shared gate를
  rejection 없이 통과했다. 완료는 tick `417`·simulation time `20.85s`, 최소 정적 여유 약
  `0.15967m`, external gate rejection `0`이었다.
- 기존 첫 rejection은 tick `89`의 `static_clearance_below_minimum`이었다. 현재 50ms 구간과
  2초 RPP rollout 자체는 안전했지만, 명시적 stop `0.215m` 앞에서도 2초 직진을 가정한 뒤 gate가
  붙인 terminal stopping 끝의 여유가 `0.0745m`로 `0.08m` 아래가 됐다.
- 안전 수치와 gate는 유지하고 RPP가 stop/terminal 앞에서 한 명령 구간 뒤 제한 감속·정지·hold
  가능한 fallback rollout을 제출하도록 수정했다.
- persistent DWB의 첫 tick에서 217개 후보·41 pose를 평가해 고른 명령을 external gate가
  다시 검사해 허용했다.
- persistent DWB 대표 종단은 tick `393`·simulation time `19.65s`, 최소 정적 여유 약
  `0.23562m`, 최대 tracking error 약 `0.04803m`, external gate rejection `0`으로 완료했다.
- DWB critic·adapter·pipeline·executor·safety·authority·timing 집중 영향권 `93 passed`를
  확인했다. 최신 전체 회귀는 실행하지 않았다.
- 첫 DWB 종단은 2cm grid의 zero tie와 계획된 회전 경계 너머 `GoalAlign` 투영 때문에
  `(1.380, 0.893)`에서 교착했다. near-goal projection을 끈 뒤 미래 세로 구간까지 현재 점수에
  섞여 `(1.548, 0.980)`으로 끌려가는 두 번째 교착도 확인했다. 현재 active translation section만
  4개 path critic에 제공해 두 원인을 닫았다.
- old tick, old window, old `stop_epoch`, binding 누락, 51 ms late, stale result는 새 비영점
  명령으로 적용되지 않았다.
- planned section stop과 protective stop을 분리했다.
- protective stop으로 `stop_epoch`가 바뀐 뒤에는 stale controller를 재호출하지 않고 HOLD를
  유지했다.

### 최신 검증 결과

```text
planned-stop regression + representative RPP E2E    3 passed
RPP/executor/pipeline/safety/authority/timing       80 passed
latest full regression                            NOT RUN
R5 public runner/reporting                         IMPLEMENTED / SMOKE + REPRESENTATIVE PASS
R5 clean full qualification receipt                NOT RUN
hidden                                             NOT USED
```

이 숫자는 표적·직접 영향권 검증이다. 최신 코드 기준 전체 회귀나 R5 public qualification을
의미하지 않는다.

---

## 3. 미완료·알려진 문제

### 해결한 최우선 문제

RPP external-gate 첫 rejection은 tick `89`에서 재현·분리·수정했다. 원인은 RPP의
planned-stop-unaware 2초 constant-command rollout이었으며 reference window, current 50ms apply,
simulator 또는 gate 안전 기준의 오류가 아니었다. 적대적 회귀와 대표 종단시험을 추가했고,
외부 gate rejection 없이 terminal completion을 확인했다.

따라서 다음을 아직 주장하면 안 된다.

- RPP·DWB 8-case 종단 비교
- R5 public qualification·receipt
- 제품 controller 채택·실제 사람 안전성

### 나머지 미완료

- R4 ready 8 case의 paired RPP/DWB public run
- clean full에서 R4 non-ready 13 case의 controller 미호출 재확인
- complete lifecycle과 qualification receipt 실제 생성 여부 확인
- R5-7 final audit·source freeze·분할 전체 회귀·결과 문서
- R5-B temporal Actor, R5-C observation-integrated comparison

### 증거 경계

- 현재 R5-A는 Actor를 제거한 정적·공간 controller 연구다.
- Python wall-clock은 기능 판정에서 제외한다. native timing은 후속 단계다.
- R2 evidence 결합 문제와 R2-B failure를 닫기 전에 R5-B/C를 제품 근거로 사용하지 않는다.
- hidden, `G1~G5`, 경로 분석 7단계 팀 결정을 시작하지 않았다.

---

## 4. 다음 작업 순서

### R5-6 남은 실행 순서

1. 구현 commit에서 source/config/catalog hash를 동결하고 읽기 전용 감사를 마친다.
2. R4 catalog 21 case 중 ready 8개는 동일 worker에서 paired RPP/DWB로, non-ready 13개는
   controller call 0으로 검증한다.
3. 독립 episode는 process 병렬로 계산하되, 쌍·seed·corpus·tick·안전·판정 의미는 바꾸지 않는다.
4. public full이 완주한 뒤에만 receipt를 쓴다. partial은 보존하되 최종 근거로 쓰지 않는다.

### R5-7

1. read-only final audit
2. R5·safety·authority·timing 직접 영향권 시험
3. source/config/corpus hash freeze
4. public run·receipt 완주 확인
5. 전체 회귀를 누락 없이 분할·process 병렬로 단 한 번 실행
6. README·TRACEABILITY·결과 문서 갱신

---

## 5. 주요 코드·문서 경로

### 우선 읽기

1. `AGENTS.md`
2. `인수인계.md`
3. `동기화_인수인계.md`
4. `docs/research/dynamic-actor-experiment/10-dynamic-local-maneuver-research-master-spec.md`
5. `docs/research/dynamic-actor-experiment/16-persistent-controller-comparison.md`
6. `docs/decisions/0012-persistent-controller-session-for-sliding-subpaths.md`
7. `docs/decisions/0013-common-reference-section-executor.md`
8. `docs/research/dynamic-actor-experiment/r4-public-local-reference-qualification-result-2026-08-14.md`
9. `simulation/path_planning_lab/README.md`
10. `simulation/path_planning_lab/TRACEABILITY.md`

### R5 코드

```text
simulation/path_planning_lab/src/hospital_path_lab/
  persistent_controller_contracts.py
  reference_section_executor.py
  persistent_rpp_controller.py
  persistent_controller_pipeline.py
  dynamic_safety.py
  local_reference_window.py
  local_algorithms/dwb_reference/persistent_adapter.py
```

### R5 시험

```text
simulation/path_planning_lab/tests/
  test_persistent_controller_contracts.py
  test_reference_section_executor.py
  test_persistent_rpp_controller.py
  test_persistent_dwb_adapter.py
  test_persistent_controller_pipeline.py
```

---

## 6. 실행 상태·체크포인트·산출물

### 실행 중 process

R5·현재 workspace의 장시간 Python 시뮬레이션은 발견하지 못했다. 확인 시점에 다른 경로의
Python process 7개가 존재했지만, executable path는 현재 repo의 `.venv`가 아니었다.

`Win32_Process.CommandLine`은 권한 거부로 읽지 못했으므로 그 process들의 정확한 작업 내용은
**미검증**이다. 인수인계 중 종료·삭제하지 않았다.

### Git에 자동 포함되는 증거

```text
simulation/path_planning_lab/outputs/
  witness-audit-public-20260813-r2-v2-4e4ba0f.zip
```

- Git tracked
- 크기: `3,657,108 bytes`
- SHA-256: `50567B093082A57232E668EF89C0316A426CD936496E465B943FE57EFA894266`
- `git pull --ff-only` 시 집 PC로 자동 동기화된다.

### Git 제외 로컬 산출물

| 경로 | 상태 | 약 크기 | 인계 필수성 |
|---|---|---:|---|
| `outputs/local-reference-public-20260814T060604Z-f43fbbf/` | R4 complete + receipt, 21/21 PASS | 64,239,359 B | 선택적; Git의 코드·결과 문서로 재생성 가능 |
| `outputs/spatial-oracle-public-20260814-53fd9f8/` | R3 complete + receipt, 21/21 PASS | 31,747,505 B | 선택적 |
| `outputs/spatial-oracle-public-dev-20260814-a/` | R3 development output | 31,158,847 B | 불필요 |
| `outputs/witness-audit-public-20260813-r2-v2-4e4ba0f/` | R2 public audit raw, hard FAIL | 85,177,510 B | 선택적; tracked ZIP이 있음 |
| `outputs/witness-audit-public-20260813-r2-v1-4e4ba0f/` | `run_state.incomplete.json` 존재 | 미계산 | 최종 근거로 사용 금지 |
| `outputs/test-runs/` | pytest temp/fixture·incomplete artifact 포함 | 미계산 | 수동 이전 불필요 |

위 `outputs/` directory는 `.gitignore`의 `/simulation/path_planning_lab/outputs/`로 제외된다.
R5 public runner는 아직 없으므로 R5 checkpoint·receipt·result directory는 존재하지 않는다.

### native 산출물

```text
simulation/path_planning_lab/src/hospital_path_lab/_native/
  dwa_core.dll  364,032 bytes
  dwa_core.lib    2,112 bytes
  dwa_core.pdb 2,519,040 bytes
```

- DLL SHA-256: `9351E9EBA2F2EC2F538696DD9AD007C42553619BFEF80DFE1B1A20A090681A12`
- `*.dll`, `*.lib`, `*.pdb`는 Git 제외대상이다.
- 현재 R5 source-derived DWB Python 기능 연구의 필수 산출물이 아니다.
- 집 PC에서 legacy/native timing이 필요하면 바이너리를 복사하기보다 해당 PC에서 재빌드한다.

---

## 7. 환경·의존성 복원

### 회사 PC에서 확인한 환경

```text
OS                    Windows
Python                3.12.10, 64-bit
pip                   26.2.1
numpy                 2.5.2
matplotlib            3.11.1
networkx              3.6.1
PyYAML                6.0.3
pytest                9.1.1
ruff                  0.16.2
hospital-path-lab     0.1.0 editable
```

`pyproject.toml`의 정본 범위는 Python `>=3.11,<3.13`이다. lock file이 없으므로 아래 range install은
기능 재현 방법이지만, 위 exact package version과 byte-for-byte 동일한 환경을 보장하지는 않는다.

### 집 PC 초기 설치

repo root PowerShell에서:

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e ".\simulation\path_planning_lab[dev]"
```

`.venv` 자체는 Git에 올리지 않고 PC별로 재생성한다.

### 선택 native build

회사 PC에는 `cmake`, `clang++`, `g++`, standalone `zig` 경로가 확인되지 않았고 Python
`ziglang` package로 빌드한 이력이 있다. `ziglang`은 `pyproject.toml`의 기본 의존성이 아니다.

```powershell
.\.venv\Scripts\python.exe -m pip install ziglang
.\.venv\Scripts\python.exe .\simulation\path_planning_lab\scripts\build_cpp_dwa_core.py
```

### 환경변수

현재 shell에서 아래 변수는 설정되지 않은 것으로 확인했다.

```text
CXX
HOSPITAL_PATH_LAB_DISABLE_CPP_DWA
HOSPITAL_PATH_LAB_CPP_DWA_LIBRARY
OMP_NUM_THREADS
OPENBLAS_NUM_THREADS
MKL_NUM_THREADS
```

필요할 때만 다음 의미로 설정한다.

```powershell
$env:HOSPITAL_PATH_LAB_DISABLE_CPP_DWA = "1"       # optional native DWA 사용 안 함
$env:HOSPITAL_PATH_LAB_CPP_DWA_LIBRARY = "C:\absolute\path\dwa_core.dll"
$env:OMP_NUM_THREADS = "1"                         # process 병렬 중 nested BLAS 과사용 방지
$env:OPENBLAS_NUM_THREADS = "1"
$env:MKL_NUM_THREADS = "1"
```

위는 예시일 뿐이고 `.env`에 저장할 비밀정보가 아니다. repo에 `.env`/인증정보 파일은
발견되지 않았다.

### Docker·VM·DB

- Dockerfile/Compose: 없음
- VM/Vagrant/Terraform: 없음
- DB migration/config: 없음
- Node service/package: 없음
- 외부 서비스·DB·container 복원: 현재 Python 연구 실험실에는 해당 없음

---

## 8. 집 PC 안전 동기화

먼저 집 PC의 `AGENTS.md`를 완전히 읽고 상태를 확인한다.

```powershell
git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes
```

집 PC에 미보존 변경이 있으면 먼저 파일명과 민감정보 여부를 확인한다. 민감정보가 없는
안전한 변경만 원격에 push하지 않는 backup branch·commit으로 보존한다.

```powershell
git switch -c backup/home-before-r5-sync-20260814
git add -- <safe-files-only>
git commit -m "chore: preserve home changes before R5 sync"
```

그런 뒤에만:

```powershell
git ls-remote origin refs/heads/main
git fetch origin --prune
git switch main
git pull --ff-only origin main
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t c66fe1d932b37a9b5afbc4a171195928afcce295
git merge-base --is-ancestor c66fe1d932b37a9b5afbc4a171195928afcce295 HEAD
git status --porcelain=v1
git diff --exit-code HEAD --
git diff --cached --exit-code HEAD --
```

합격 조건:

- `HEAD == origin/main`
- `c66fe1d...` object type `commit`
- ancestor check exit 0
- status 출력 없음
- 두 diff exit 0

fast-forward가 실패하거나 `main`/`origin/main`이 갈라지면 reset·rebase·merge로 임의 해결하지 말고
양쪽 커밋과 영향 파일을 보고한다.

금지:

```text
git reset --hard
git clean
git rebase
git push --force
무단 git stash
```

---

## 9. 복원 후 빠른 검증

먼저 정적 검사와 표적 시험만 실행한다. 전체 회귀는 R5-7 final audit 뒤 한 번만
분할 병렬로 실행한다.

```powershell
.\.venv\Scripts\python.exe -m ruff check .\simulation\path_planning_lab

$testRun = ".\simulation\path_planning_lab\outputs\test-runs\home-r5-pipeline-$([guid]::NewGuid())"
.\.venv\Scripts\python.exe -m pytest `
  -c .\simulation\path_planning_lab\pyproject.toml `
  .\simulation\path_planning_lab\tests\test_persistent_controller_pipeline.py `
  -p no:cacheprovider `
  --basetemp=$testRun `
  -q
```

R5·DWB 직접 영향권:

```powershell
$testRun = ".\simulation\path_planning_lab\outputs\test-runs\home-r5-direct-$([guid]::NewGuid())"
.\.venv\Scripts\python.exe -m pytest `
  -c .\simulation\path_planning_lab\pyproject.toml `
  .\simulation\path_planning_lab\tests\test_reference_section_executor.py `
  .\simulation\path_planning_lab\tests\test_persistent_rpp_controller.py `
  .\simulation\path_planning_lab\tests\test_persistent_dwb_adapter.py `
  .\simulation\path_planning_lab\tests\test_persistent_controller_contracts.py `
  .\simulation\path_planning_lab\tests\test_local_reference_window.py `
  .\simulation\path_planning_lab\tests\test_dynamic_trajectory_constraints.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_goal_controller.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_generator.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_critics.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_core.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_composition.py `
  .\simulation\path_planning_lab\tests\test_dwb_reference_adapter.py `
  -p no:cacheprovider `
  --basetemp=$testRun `
  -q
```

예상 기준은 직전 회사 PC에서 `195 passed`였다. 패키지 버전 차이로 달라지면 즉시 코드를
수정하지 말고 interpreter·dependency·실패 노드부터 보고한다.

---

## 10. 커밋 대상과 수동 이전 구분

### Git commit/push 대상

- R5-1~R5-5 코드·시험·문서
- R3/R4 결과 문서와 재현 코드
- tracked R2 witness ZIP
- 본 `동기화_인수인계.md`

### 수동 이전이 필요하거나 선택적인 항목

- 필수 수동 이전: **없음**
- 선택 수동 이전: R3/R4 raw `outputs/` directory
- 선택 수동 이전: legacy native DLL/LIB/PDB; PC ABI 차이를 고려하면 재빌드 권장
- 선택 수동 이전: local-only legacy branch `0f9654e`; 현재 R5 재개에는 불필요
- 이전하지 말 것: `.venv`, pytest temp, incomplete output, credentials, `.env`, token/key

---

## 11. 집 PC 작업 재개 프롬프트

아래를 집 PC의 기존 작업 세션에 그대로 붙여 넣는다.

```text
hospital-autonomous-wheelchair 프로젝트의 2026-08-14 회사 PC R5 작업을 집 PC에서 이어받는다.

정본:
- repository: https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
- branch: main
- required implementation ancestor: c66fe1d932b37a9b5afbc4a171195928afcce295
- required implementation tree: 8eee0570929761ca62c8409bef3f22fe23799200

먼저 AGENTS.md를 완전히 읽고, 집 로컬의 변경·stash·local-only commit을 읽기 전용으로 확인해라.
변경을 삭제하거나 덮어쓰지 마라. 민감정보가 없는 미보존 변경은 원격에 push하지 않는
backup branch와 commit으로 먼저 보존해라.

금지:
- git reset --hard
- git clean
- rebase
- force push
- 무단 stash

보존 후 다음으로 동기화해라.

git ls-remote origin refs/heads/main
git fetch origin --prune
git switch main
git pull --ff-only origin main
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t c66fe1d932b37a9b5afbc4a171195928afcce295
git merge-base --is-ancestor c66fe1d932b37a9b5afbc4a171195928afcce295 HEAD
git status --porcelain=v1

HEAD와 origin/main이 같고, c66fe1d가 HEAD의 ancestor이며, 작업트리가 clean인지 확인해라.
fast-forward가 실패하거나 브랜치가 갈라지면 임의로 해결하지 말고 차이를 보고해라.

동기화 후 다음을 순서대로 완전히 읽어라.
1. AGENTS.md
2. 인수인계.md
3. 동기화_인수인계.md
4. docs/research/dynamic-actor-experiment/10-dynamic-local-maneuver-research-master-spec.md
5. docs/research/dynamic-actor-experiment/16-persistent-controller-comparison.md
6. docs/decisions/0012-persistent-controller-session-for-sliding-subpaths.md
7. docs/decisions/0013-common-reference-section-executor.md
8. docs/research/dynamic-actor-experiment/r4-public-local-reference-qualification-result-2026-08-14.md
9. simulation/path_planning_lab/README.md
10. simulation/path_planning_lab/TRACEABILITY.md

환경은 Python 3.12 venv를 새로 만들고
  .\.venv\Scripts\python.exe -m pip install -e ".\simulation\path_planning_lab[dev]"
로 복원해라. Docker·VM·DB·.env는 현재 필요 없다. native DWA는 R5-6 필수 의존성이 아니다.

인수할 현재 상태:
- R5-1~R5-6 구현 완료, R5-A clean public full은 미실행
- planned-stop regression과 대표 RPP 종단 3 passed
- RPP/executor/pipeline/safety/authority/timing 집중 영향권 80 passed
- RPP 60-tick prefix와 full external-gate terminal completion 통과
- DWB 첫 tick 217 candidates/41 poses external-gate 재검사와 full terminal completion 통과
- DWB 대표 종단 tick 393, 19.65s, gate rejection 0
- RPP tick 89 false rejection은 planned-stop-unaware rollout으로 원인 확정·수정 완료
- R5 8-case full run, receipt, full regression 미실행
- hidden 미사용

R5-6 runner/reporting은 구현됐다. tick-1 21-case smoke는 ready 8쌍·non-ready 13 무호출,
대표 full pair는 RPP tick 417·DWB tick 393·hard/deadlock 0이었다. 다음 작업은 clean commit에서
R5-A public full을 단 한 번 실행해 8쌍 종단·관계·repeat·serial/process parity와 receipt 여부를
확인하는 것이다. Python wall-clock은 기능 판정에서 제외해라.

표적 진단·감사·대표 public이 통과하기 전에 장시간 전체 회귀를 시작하지 마라.
hidden, 제품 알고리즘 채택, G1~G5, 경로 분석 7단계 팀 결정을 시작하지 마라.

첫 답변에서는 다음만 간결히 보고해라.
1. 동기화 전후 branch/HEAD/origin/main
2. backup 필요 여부
3. working tree clean 여부
4. c66fe1d ancestor 확인
5. Python/venv 복원 여부
6. R5 완료·미완료 인수 결과

그 뒤 R5-6 작업 범위를 보고하고 시작해라.
```
