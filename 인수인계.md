# 병원 자율주행 휠체어 프로젝트 인수인계

> **2026-08-12 집 PC 후속 작업:** 회사 PC 인계 뒤 수행한 DWB 지역 우회 탐색,
> 180-tick Ideal 부분 결과, checkpoint 한계와 Pro·Claude 연구 검토는
> `docs/reviews/home-local-dwb-research-handoff-2026-08-12.md`가 최신이다.
> 기존 본문은 회사→집 인계 당시 기준으로 보존한다.
>
> **2026-08-13 후속 연구 방향:** Pro 최초 판정, Claude Opus 5 Max 문헌조사와
> Claude 자료를 받은 Pro 재판정의 출처·보정사항·다음 공개 연구 순서는
> `docs/reviews/dynamic-local-maneuver-research-direction-2026-08-13.md`를 따른다.
> 이는 사용자 개인 연구 방향이며 팀 합의·제품 알고리즘 채택이 아니다.

## 1. 문서 목적

이 문서는 2026-08-12 회사 PC에서 수행한 다음 작업을 GitHub `main`을 통해 집 PC로
인계하기 위한 현재 기준 문서다.

- 공개 구현을 참고한 source-derived DWA/DWB 기준선 작성
- 사람의 진행 방향을 반영하는 v7 방향성 Actor 예측
- 휠체어 직사각형 footprint와 Actor Capsule의 정확한 충돌 검사
- shared safety gate와 source-derived DWB 연결
- 공개 1개 시나리오의 실제 폐루프 우회 사전 진단
- 회사 PC에서 중단한 지점과 집 PC 재개 순서

이 작업은 모두 Python `simulation_only` 연구다. 실제 사람은 태우지 않았고, 제품
알고리즘 채택, `G1~G5`, 경로 분석 7단계, 의료기기 인증과 실제 사람 탑승 안전성을
결정하지 않았다.

---

## 2. Git 정본과 동기화 자격증명

| 항목 | 값 |
|---|---|
| 저장소 | `https://github.com/shotgun1107/hospital-autonomous-wheelchair.git` |
| 기본·정본 브랜치 | `main` |
| 회사 작업 브랜치 | `codex/source-derived-dwb-directional-v7-handoff` |
| 작업 시작 기준 | `dc49d0a6dad695b032f384d4ee29ca7321d434fa` |
| 구현 커밋 | `340cb400ca51229fcd7c032b97a98369c757586f` |
| 구현 커밋 tree | `a4c09892066124e18b5fc0c21d5969285d66adfe` |
| GitHub 계정 | `shotgun1107` |

회사 PC의 `gh auth status`는 만료된 GitHub CLI 토큰을 보고했다. 그러나 Windows Git
Credential Manager의 HTTPS 자격증명은 유효했고 다음 명령으로 원격 접근을 확인했다.

```powershell
git ls-remote origin refs/heads/main
```

따라서 회사 PC push는 `gh` 토큰을 우회해 Git Credential Manager로 수행한다. 토큰이나
비밀번호를 문서·commit·프롬프트에 직접 쓰지 않는다. 집 PC에서 Git 원격 접근까지 실패할
때만 다음과 같이 `shotgun1107` 계정을 다시 인증한다.

```powershell
gh auth login -h github.com
# HTTPS → GitHub credentials 사용 → 웹 브라우저 인증
```

완료 구현은 작업 브랜치에서 커밋한 뒤 `main`에 fast-forward 병합하고 `origin/main`까지
push한다. 집 PC는 작업 브랜치가 아니라 `main`을 받는다.

---

## 3. 과거 — 왜 다시 설계했는가

### 3.1 기존 사용자 정의 DWA 결과를 잘못 해석할 위험

기존 v6 C++ DWA는 50 ms timing 하위 자격을 통과했지만 expanded public 기능 자격에서
실제 우회가 확인되지 않았다. 공개 결과는 DWA feasible detour `0/11`, 전체 DWA 양의
detour `0/98`이었다.

이 결과는 `DWA 알고리즘 자체가 나쁘다`는 증거가 아니었다. 다음 문제가 섞여 있었다.

- 공개 개발시험에서 실제 우회를 확인하기 전에 hidden으로 넘어가던 과거 게이트
- 후보가 왜 탈락했는지 충분히 보존하지 않은 진단 구조
- 미완료 episode에서 측면 이탈이 있어도 `positive_detour_length_m=0`이 되기 쉬운 지표
- evaluator witness와 실제 DWA action primitive·2초 rollout 계약의 차이
- 정지·재개 뒤 좁은 angular window와 직진 선호 비용
- 당시 Actor 예측이 진행 방향과 무관하게 반전까지 가능한 큰 원으로 팽창한 문제

따라서 기존 결과는 다음처럼 해석한다.

> 기존 사용자 정의 DWA 구현은 공개 기능 자격을 통과하지 못했다. 이것은 특정 구현과
> 실험계약의 실패이며 DWA 계열 전체의 폐기 근거가 아니다.

### 3.2 공개 DWA/DWB 구현을 기준으로 다시 작성

임의 구현을 계속 덧대는 대신 다음 공개 소스를 고정해 구조와 의미를 분석했다.

- ROS1 `navigation` commit `f44bb1fc2810399165115cc98b530fe4b9397c18`
- Nav2 `navigation2` commit `1e8afb17e2e09df443b1870ce0f4ecdee32207fd`

복사한 제품 플러그인이 아니라, 공개 소스의 generator·critic·core 수명주기와 선택 규칙을
우리 Python 실험 계약으로 재구성했다. 프로젝트의 terminal stopping, Actor tube,
forbidden zone과 외부 shared safety gate는 더 강한 기존 계약이므로 제거하지 않았다.

### 3.3 사람 방향 관성이 빠졌던 문제

기존 Actor 예측은 관측 속도로 중심을 이동시키면서도 불확실성 반경에는 즉시 정지·횡이동·
반대 방향 전환을 모두 넣었다. 같은 방향으로 천천히 가는 Actor도 2초 뒤에는 큰 원으로
퍼져 넓은 복도에서 모든 DWB 후보를 막았다.

실제 첫 공개 진단은 다음과 같았다.

```text
기존 원형 예측:
217 / 217 후보가 Actor clearance로 탈락
```

이 때문에 방향 관성을 별도 v7 연구 계약으로 추가했다.

---

## 4. 현재 — 완료된 구현

### 4.1 source-derived DWB 기준선

다음 구성요소를 분리해 구현했다.

- dynamic window와 `v × w` 후보 생성
- 후보당 2.0초, 0.05초 간격, 초기 pose 포함 41 pose rollout
- critic lifecycle: `prepare → score → debrief → reset`
- PathDist, GoalDist, PathAlign, GoalAlign, Oscillation, RotateToGoal critic
- strict lowest-cost 선택과 결정론적 generator-order tie-break
- goal 접근 감속·정지·제자리 회전 상태기계
- `ControllerSnapshot` adapter와 전체 semantic digest
- project dynamic safety constraint critic
- 선택 후보를 다시 검사하는 외부 shared safety gate 유지

구현 위치:

```text
simulation/path_planning_lab/src/hospital_path_lab/local_algorithms/dwb_reference/
simulation/path_planning_lab/src/hospital_path_lab/dynamic_trajectory_constraints.py
```

이 구현은 Python reference다. Nav2 plugin, ROS 2 제품 controller 또는 사람 탑승 증거가
아니다.

### 4.2 방향성 Actor 예측 v7

현재 v7은 `constant-heading open-loop synthetic circular Actor`만 대상으로 한다.

```text
10 Hz validated observation
→ Actor binding별 서로 다른 관측 20개
→ 관측 속도 벡터 평균
→ norm(mean_velocity) - 2σ >= 0.03 m/s 확인
→ 최신 관측 위치를 기준점으로 방향 확정
→ 후진하지 않는 종방향 reachable interval
→ 정확한 Capsule 생성
```

주요 규칙:

- 20 Hz controller가 같은 10 Hz frame을 두 번 보더라도 이력은 한 번만 증가한다.
- stale, invalid, unavailable, 순서 역행과 track/binding 변경에서 이력을 폐기한다.
- 방향 확정 전에는 추측하지 않고 정지한다.
- fresh EMPTY와 관측 dropout을 구분한다.
- 방향이 확정된 Actor는 제한 감속 후 정지할 수 있지만 즉시 뒤로 가지 않는다.
- 진행 방향의 제한 가속은 최대 `0.50 m/s`까지 반영한다.
- 현재 lateral turn bound는 `0`이다.
- `2σ`는 연구용 휴리스틱이며 실제 사람 안전 확률 보장이 아니다.

구현 위치:

```text
simulation/path_planning_lab/src/hospital_path_lab/dynamic_directional_prediction.py
simulation/path_planning_lab/src/hospital_path_lab/dynamic_directional_experiment.py
```

Normal 공개 same-direction-wide 5개는 방향을 확정했다. 느린 Actor와 큰 noise가 결합된
Stress 5개는 `READY 0`이며 보수 정지한다.

### 4.3 정확한 Capsule 충돌과 안전 계약

휠체어는 방향을 가진 직사각형 footprint, Actor reachable region은 선분과 원의 Minkowski
sum인 Capsule로 계산한다.

- 직사각형 ↔ Capsule signed surface clearance
- Capsule 선분 길이 0이면 기존 circle 계산과 정확히 동일
- apply latency 50 ms current-motion sweep
- 후보 rollout 전체
- terminal stopping sweep
- 모든 swept 구간을 최대 5 ms 간격으로 검사
- physical obstacle, forbidden zone, Actor `0.08 m` clearance 유지

다음 적대 입력도 fail-closed로 고쳤다.

- 과거 `FRESH` snapshot과 prediction 재사용
- direction prediction 도중 no-frame dropout
- 동결되지 않은 prediction parameter 주입
- prediction geometry·history·hash·객체 복사 또는 변조
- 잘못된 Actor prediction 타입
- 7 ms trajectory 구간에서 5 ms sweep를 건너뛰는 경우

독립 최종 감사 결과는 `P0 없음`, `잔여 P1 없음`이다.

### 4.4 검증 결과

회사 PC에서 구현 커밋 직전 확인한 결과:

```text
전체 pytest: 549 passed in 444.05s
Ruff: All checks passed
git diff --check: passed
```

대용량 `simulation/path_planning_lab/outputs/`는 Git에 커밋하지 않았다.

---

## 5. 중단한 실제 폐루프 시험

대상은 공개 development `same-direction-wide-r00`, Normal observation, source-derived DWB다.
hidden, runner, C++/Cython과 제품 인터페이스는 사용하지 않았다.

### 5.1 첫 READY tick

```text
방향 확정: tick 42, simulation time 2.10 s
후보: 217
완전 평가 legal: 52
score short-circuit: 165
illegal: 0
선택: candidate 165
status: FOUND
Python controller 시간: 약 7.33 s / tick
```

방향성 Capsule 적용으로 기존 `217/217 Actor clearance 탈락`은 해소됐다.

### 5.2 66-tick partial

```text
첫 재개 허용: tick 62
첫 실제 명령: v=0.0104167 m/s, w=-0.0266667 rad/s
tick 65 명령: v=0.0420139 m/s, w=-0.0213333 rad/s
실제 이동 tick: 4
final pose: x=0.60314235, y=2.31999454, yaw≈-0.004
maximum reference deviation: 약 0.00000546 m
gate override: 20
controller stop request: 42
no-safe candidate: 0
```

tick 42부터 합법 후보가 있었지만 즉시 움직이지 않은 것은 오류가 아니다. 보호정지 뒤 현재
`stop_epoch` 재승인과 서로 다른 안전 관측 11개가 필요해서 tick 62에 처음 재개됐다.

### 5.3 중단 지점

100-tick prefix를 실행하다 회사 작업시간 종료로 중단했다. 프로세스는 명시적으로 종료했고
생성 중이던 partial을 최종 실험 근거로 사용하지 않는다. 66-tick 결과도 다음만 증명한다.

- 방향 확정 뒤 합법 후보가 생성됨
- shared gate가 11개 새 관측과 현재 epoch 재승인을 기다림
- 재개 뒤 차체가 실제로 출발함

다음은 아직 증명되지 않았다.

- reference에서 `0.10 m` 이상 실제 이탈
- Actor가 존재하는 동안 ordered overtake
- sustained rejoin
- 목표 도착
- source-derived DWB의 50 ms 자격

정규 pytest에는 첫 READY 후보 자격만 포함한다. 900-tick 장기 시험 함수는 다음 파일에
수동 재개용으로 보존했으며 자동 회귀에서 수집하지 않는다.

```text
simulation/path_planning_lab/tests/test_directional_dwb_focused_public.py
```

---

## 6. 집 PC 동기화 절차

### 6.1 로컬 변경 보존

```powershell
Set-Location '<집 PC 저장소 경로>'
Get-Content -LiteralPath AGENTS.md -Raw -Encoding UTF8
git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes
```

집 PC 변경을 삭제하거나 덮어쓰지 않는다. `.env`, token, key, credential 등 민감 파일이
보이면 stage하지 말고 중단한다. 민감하지 않은 미보존 변경이 있으면 원격에 push하지 않는
backup 브랜치와 commit으로 먼저 보존한다.

```powershell
git switch -c backup/home-before-directional-v7-sync-20260812
git add -A
git commit -m "backup home work before directional v7 sync"
```

`reset --hard`, `clean`, rebase, force push와 무단 stash는 금지한다.

### 6.2 원격 접근과 main 동기화

```powershell
git ls-remote origin refs/heads/main
git fetch origin --prune
git switch main
git pull --ff-only origin main
```

`git ls-remote`가 인증 때문에 실패하면 그때만 `gh auth login -h github.com`으로
`shotgun1107` 계정을 웹 인증한다. `gh auth status`가 실패해도 `git ls-remote`가 성공하면
Git Credential Manager 자격증명으로 동기화할 수 있다.

fast-forward가 실패하거나 `main`과 `origin/main`이 갈라지면 reset·rebase·임의 merge하지
말고 양쪽 commit과 영향을 받는 파일을 보고한다.

### 6.3 동기화 검증

```powershell
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t 340cb400ca51229fcd7c032b97a98369c757586f
git merge-base --is-ancestor 340cb400ca51229fcd7c032b97a98369c757586f HEAD
git rev-parse '340cb400ca51229fcd7c032b97a98369c757586f^{tree}'
git status --porcelain=v1
git diff --exit-code HEAD --
git diff --cached --exit-code HEAD --
```

통과 조건:

- `HEAD == origin/main`
- 구현 object type은 `commit`
- 구현 commit은 `HEAD`의 ancestor
- 구현 tree는 `a4c09892066124e18b5fc0c21d5969285d66adfe`
- status 출력 없음
- 두 diff 성공

---

## 7. 집 PC에서 이어서 할 작업

1. Git 동기화와 문서 인수만 먼저 확인한다.
2. 전체 549개를 곧바로 반복하기보다 다음 집중 시험부터 확인한다.
3. 새 controller와 새 context factory로 100-tick public prefix를 처음부터 다시 실행한다.
4. 선택 명령, 실제 pose, 최대 경로 이탈, legal/illegal taxonomy와 gate override를 기록한다.
5. 이탈이 발생하면 시간을 단계적으로 늘려 ordered overtake와 sustained rejoin을 확인한다.
6. 실패하면 Python source-derived controller만 원인 추적·수정하고 같은 공개 시험을 반복한다.
7. 실제 우회·추월·재합류·도착이 확인되기 전에는 C++로 이식하지 않는다.
8. 공개 기능이 통과한 뒤에만 동일 의미 C++ 이식과 별도 직렬 50 ms 자격을 진행한다.

집중 회귀:

```powershell
.\.venv\Scripts\python.exe -m pytest -q `
  simulation\path_planning_lab\tests\test_dynamic_directional_prediction.py `
  simulation\path_planning_lab\tests\test_dynamic_directional_experiment.py `
  simulation\path_planning_lab\tests\test_directional_public_qualification.py `
  simulation\path_planning_lab\tests\test_dynamic_safety.py `
  simulation\path_planning_lab\tests\test_dwb_reference_adapter.py `
  simulation\path_planning_lab\tests\test_dwb_reference_composition.py `
  simulation\path_planning_lab\tests\test_directional_dwb_focused_public.py
```

실험 규칙:

- 한 episode 내부 tick은 앞 상태가 다음 상태로 이어지므로 직렬 실행한다.
- 서로 독립적인 여러 episode는 process 기반 병렬 실행할 수 있다.
- wall-clock timing qualification은 CPU 간섭 없이 별도 직렬 실행한다.
- 후보 217개, 후보당 41 pose, 2초 rollout, terminal stopping, 5 ms sweep, `0.08 m`
  clearance, 비용·순위·tie-break와 shared safety gate를 결과에 맞춰 완화하지 않는다.
- evaluator category, oracle, hidden label과 ground truth를 controller 입력에 넣지 않는다.
- public 기능 자격 전에는 hidden을 생성·조회·실행하지 않는다.
- 기존 hidden을 새 최종 증거로 재사용하지 않는다.

다음 Actor 범위는 여전히 후속이다.

- 30°·45°·90° 방향 전환
- 정지 후 재출발
- 반전
- 사람이 로봇을 보고 반응하는 상호작용

이를 추가하기 전에는 현재 v7을 실제 보행자 예측으로 확대 해석하지 않는다.

---

## 8. 같은 Codex 작업에 붙여 넣을 시작 프롬프트

```text
hospital-autonomous-wheelchair 프로젝트의 회사 PC 작업을 집 PC에서 이어받는다.

정본:
- repository: https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
- default branch: main
- GitHub account: shotgun1107
- required implementation commit: 340cb400ca51229fcd7c032b97a98369c757586f
- required implementation tree: a4c09892066124e18b5fc0c21d5969285d66adfe

먼저 현재 PC의 AGENTS.md와 상위 AGENTS.md를 완전히 읽고 따른다.
그다음 구현하지 말고 로컬 상태를 확인한다.

git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes

로컬 변경을 삭제하거나 덮어쓰지 마라. 민감 파일이 없다면 미보존 변경은 원격에 push하지
않는 backup 브랜치와 backup commit으로 먼저 보존한다. reset --hard, clean, rebase,
force push와 무단 stash는 금지한다.

원격 접근은 먼저 다음으로 확인한다.

git ls-remote origin refs/heads/main

회사 PC에서는 gh CLI 토큰이 만료됐지만 Git Credential Manager의 HTTPS 자격증명으로
접근했다. 집 PC에서도 git ls-remote가 성공하면 gh auth status 실패와 무관하게 Git 동기화가
가능하다. git 원격 접근까지 실패할 때만 `gh auth login -h github.com`을 실행하고 HTTPS,
GitHub credentials, web browser 방식으로 shotgun1107 계정을 인증한다. 토큰을 파일이나
채팅에 직접 출력하지 마라.

보존 후 또는 처음부터 깨끗하면 다음을 실행한다.

git fetch origin --prune
git switch main
git pull --ff-only origin main
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t 340cb400ca51229fcd7c032b97a98369c757586f
git merge-base --is-ancestor 340cb400ca51229fcd7c032b97a98369c757586f HEAD
git rev-parse '340cb400ca51229fcd7c032b97a98369c757586f^{tree}'
git status --porcelain=v1
git diff --exit-code HEAD --
git diff --cached --exit-code HEAD --

통과 조건:
- HEAD와 origin/main 일치
- implementation object type은 commit
- implementation commit은 HEAD의 ancestor
- implementation tree는 a4c09892066124e18b5fc0c21d5969285d66adfe
- 작업트리 clean

fast-forward가 실패하거나 main과 origin/main이 갈라지면 임의로 해결하지 말고 local main,
origin/main, 양쪽에만 있는 commit, backup 브랜치와 영향 파일을 보고한다.

동기화 뒤 다음 문서를 순서대로 완전히 읽는다.
1. AGENTS.md
2. 인수인계.md
3. docs/research/dynamic-actor-experiment/07-open-source-dwa-dwb-analysis-and-adaptation.md
4. docs/decisions/0009-source-derived-dwa-dwb-reference.md
5. docs/research/dynamic-actor-experiment/08-directional-actor-prediction-v7.md
6. docs/decisions/0010-directional-actor-prediction.md
7. docs/research/dynamic-actor-experiment/04-controller-integration.md
8. docs/research/dynamic-actor-experiment/05-evaluator-and-corpus.md
9. docs/research/dynamic-actor-experiment/06-runner-hidden-and-reporting.md
10. simulation/path_planning_lab/README.md
11. simulation/path_planning_lab/TRACEABILITY.md

인수할 상태:
- source-derived DWB Python reference와 방향성 Actor v7이 구현됐다.
- v7은 constant-heading open-loop synthetic circular Actor 전용이다.
- 20개 unique observation으로 방향을 확정하고 불확실하면 정지한다.
- 방향 Actor는 no-reverse longitudinal Capsule로 예측한다.
- exact oriented wheelchair footprint↔Capsule clearance를 apply, rollout, terminal에 적용한다.
- stale replay, dropout, parameter/hash/history/geometry tamper는 fail-closed다.
- 전체 회귀 549 passed, Ruff passed, 최종 독립 감사 P0/P1 없음이다.
- 공개 same-direction-wide-r00 첫 READY에서 217개 중 52개 legal 후보가 생성됐다.
- 66-tick partial에서 tick62 재승인 뒤 4 tick 실제 이동을 확인했다.
- 100-tick 실행은 회사 시간 종료로 중단했으며 partial 결과는 최종 근거가 아니다.
- 실제 0.10m 이탈, Actor 존재 중 추월, sustained rejoin, 목표 도착은 아직 미증명이다.
- Python DWB는 첫 READY tick 약 7.33초로 느리며 50ms 자격 대상이 아니다.
- hidden, full runner, receipt, 제품 알고리즘 채택과 G1~G5는 수행하지 않았다.

먼저 동기화 결과와 위 상태 인수를 간결하게 보고하라. 그다음 사용자의 추가 승인 질문 없이
다음 작업을 이어서 진행한다.

1. 집중 방향/DWB 회귀를 실행한다.
2. 새 controller와 새 context factory로 공개 same-direction-wide-r00 Normal 100-tick prefix를
   처음부터 실행한다.
3. legal/illegal 후보, 선택 v/w, 실제 pose, max reference deviation, gate override와 no-safe
   원인을 기록한다.
4. 실제 측면 이탈이 시작되면 tick 범위를 단계적으로 늘려 Actor 존재 중 ordered overtake,
   sustained rejoin과 goal completion을 검증한다.
5. 실패하면 최대 10회까지 결과 역추적→Python source-derived controller의 최소 수정→같은
   공개 시험 재실행을 반복한다.

시간 제한 50ms는 이 행동 검증에서 적용하지 않는다. 실제 우회 기능이 먼저다. C++/Cython,
native DWA, runner, hidden, corpus label, evaluator 기준, safety threshold, 후보 수, rollout,
terminal stopping과 shared gate는 수정하지 마라. Python으로 공개 1개 기능시험을 완전히
통과한 뒤에만 C++ 이식을 검토한다.

한 episode 내부 tick은 상태가 이어지므로 직렬로 실행한다. 여러 독립 episode를 나중에
평가할 때만 process 병렬화를 사용하고, wall-clock timing은 항상 별도 직렬로 측정한다.
60초 이상 계산이 계속되면 현재 tick, 진행률과 관찰값을 중간 보고한다.
```

---

## 9. 한 줄 인계

```text
source-derived DWB와 20-frame 방향성 Capsule 예측은 구현·549회귀·안전감사를 통과했고,
공개 1개에서 합법 후보와 재승인 뒤 실제 출발까지 확인했다. 집 PC에서는 100-tick부터 다시
시작해 실제 이탈→추월→재합류→도착을 Python으로 먼저 증명하며, 그 전에는 C++·hidden·제품
채택으로 넘어가지 않는다.
```
