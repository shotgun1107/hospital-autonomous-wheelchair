# 병원 자율주행 휠체어 프로젝트 회사→집 PC 인수인계

## 최신 상태 — R2-B 실제 카메라 입력 대기

- 작업일: `2026-08-16`
- 집 PC와 저장소에 인식된 카메라 장치, 영상, 검출·추적 모델, 카메라 연결 코드가 없다.
- 따라서 실제 frame·FOV·가림·사람 검출·track 지속성 시험은 시작하지 않았다.
- 다음 최소 입력은 실제 카메라 연결, Windows 장치명 또는 모델명, 해상도·frame rate, 같은
  설치 상태의 빈 장면·횡단 사람·같은 방향 사람 영상 각 10~20초다.
- 입력 상실·좌표 변환 불가·가시영역 불명에서는 `ActorTrack`을 만들거나 이동 근거로 쓰지
  않고 출발 금지 또는 보호정지로 닫는다.
- 상세: `docs/research/dynamic-actor-experiment/23-r2b-camera-input-integration-gate.md`

## 최신 상태 — R5-C 통과 후 원 경로 복귀, 공개 Normal 좌·우 완료

- 작업일: `2026-08-16`
- 공개 Normal 횡단에서 post-pass 증거 뒤 관측을 잃으면 실제 정지하고, 기존 우회 경로를
  반복하지 않고 새 stop epoch·현재 pose에 묶인 `FOLLOW_ORIGINAL` 경로를 만든다.
- 왼쪽은 post-pass tick `632`, 복귀 release `680`, 완료 tick `1328`이다.
- 오른쪽은 post-pass tick `633`, 복귀 release `680`, 완료 tick `1432`이다.
- 오른쪽 종점 교착 원인은 마지막 위치 이동과 최종 방향 맞춤을 한 목표에 묶은 경로
  표현이었다. 방향 차이가 기존 `0.08rad`보다 클 때 두 section으로 분리해 해결했다.
- Stress 좌·우 1600틱은 방향 판단 준비가 되지 않아 release·controller call·이동 `0`,
  `HOLDING`, hard failure `0`으로 닫혔다. post-pass 증거 없는 EMPTY release 버그도 막았다.
- 양쪽 최소 Actor/정적 여유는 각각 `0.5775/0.3793m`, `0.5970/0.3786m`, hard failure `0`이다.
- DWB 후보·비용·rollout, 안전거리 `0.08m`, prediction과 shared gate는 변경하지 않았다.
- 도착 허용값, DWB 후보·점수·rollout과 안전 기준은 늘리거나 완화하지 않았다.
- 실제 카메라·FOV·검출, formal R5-C/R6, hidden, 제품 알고리즘·G1~G5·제품
  경로분석 7단계는 미수행이다.
- 상세: `docs/research/dynamic-actor-experiment/r5c-post-pass-return-result-2026-08-16.md`

## 최신 상태 — 초음파 전환 철회와 카메라 입력 가정의 횡단 경로 복구

- 작업일: `2026-08-16`
- 초음파 전환 문서·가정 하네스·7개 HC-SR04 감사 3개 커밋은 `git revert`로 제거했다.
  카메라 등 상위 관측 영역이 기존 `ActorTrack`을 제공한다고 두고 경로 연구를 계속한다.
- 보호정지 뒤 현재 pose·새 stop epoch·새 유효 tick에 묶인 횡단 reference/controller session을
  다시 만들고 현재 local window section에서 실행하도록 했다.
- 추월 완료 판정이 관측 잡음으로 되돌아가면 실행 오류가 아니라 보호정지로 닫고, world 종료
  전 20 tick은 실제 정지 확인에 사용한다.
- Normal 공개 횡단 좌·우 복구 시험이 각각 통과했다. 이는 중단 후 경로 복구의 제한 증거이며
  실제 카메라·FOV·검출 성능이나 제한 시간 내 항상 도착을 증명하지 않는다.
- 검증: reference/executor `22 passed`, 왼쪽 `1 passed in 70.48s`, 오른쪽
  `1 passed in 67.86s`, Ruff 통과. 전체 회귀·hidden·receipt는 미실행이다.
- 상세: `docs/research/dynamic-actor-experiment/22-r5c-crossing-recovery.md`,
  `docs/research/dynamic-actor-experiment/r5c-crossing-recovery-result-2026-08-16.md`

## 최신 상태 — R2-B 추상 감시 진입 구간 보정

- 작업일: `2026-08-16`
- 원본 v6 `second-risk`와 legacy `dynamic-change` world·hash·hard failure는 삭제하거나
  덮어쓰지 않았다.
- 지연 출현 Actor를 원래 진입 상태에서 `t=0`까지 같은 속도로 역산한 별도 감시 접근 world를
  구현했다. 모든 지연 Actor, 10Hz 20-frame, Ideal 100ms 지연을 포함한 최소 `2.0s` lead와
  진입 상태 연속성을 검증한다.
- 대표 replay는 원본 containment miss `38/22`를 재현했고 파생 world는 `0/0`, Ideal hard
  failure `0/0`이다. 원본 전체 audit의 legacy `43`은 다른 witness 묶음의 역사적 수치로 유지한다.
- 전용·영향권 시험은 합계 `88 passed`, 실패 `0`이다. 전체 회귀는 4개 process shard 합계
  `935 passed`, 실패·건너뜀 `0`이며 Ruff·compileall·`git diff --check`도 통과했다.
- 이 결과는 로컬 주행 지도 밖 감시 접근을 허용한 추상 simulation 계약이다. 실제 카메라·FOV·
  가림·검출 성능, Normal·Stress 종단, formal R5-C/R6, hidden과 제품 자격이 아니다.
- 상세:
  `docs/research/dynamic-actor-experiment/21-r2b-monitored-entry-coverage.md`
  `docs/research/dynamic-actor-experiment/r2b-monitored-entry-coverage-result-2026-08-16.md`
- 다음: 전체 회귀 뒤, 횡단 PASS 관측 복구 또는 실제 카메라 coverage 연구 중 별도 승인된
  범위만 진행한다. 제품 알고리즘·G1~G5·제품 경로분석 7단계는 시작하지 않는다.

## 최신 상태 — R2-B 원인 재확인과 R5-C 제한 복구 구현

- 작업일: `2026-08-16`
- 기존 R2-B hard failure 2건을 문서·공개 corpus·Ideal 관측으로 다시 확인했다. 원인은
  v6 `second-risk`와 legacy `dynamic-change`의 지도 내부 Actor 순간 출현과 관측 지연이다.
  실제 Actor가 생긴 시점에도 최신 frame이 직전 시점의 fresh `EMPTY`일 수 있다.
- 이 문제는 DWA/DWB나 Capsule 수식 결함이 아니다. 실제 카메라·가시영역·진입 감시 계약이
  없는 상태에서 반경·지연·안전 기준을 바꾸지 않았고, R2-B 실패 2건도 삭제하지 않았다.
- 이미 추적하던 Actor의 관측 상실은 별도 제한 복구로 구현했다. Normal 다중 위험에서
  prediction loss `100, 184, 306, 384, 494, 624, 666`, 실제 정지
  `111, 198, 320, 398, 508, 629, 675`, 새 session release
  `82, 138, 220, 356, 424, 618, 650`을 확인했다.
- 각 새 session은 stop epoch `1~7`에 결박된 새 현재-pose reference와 새 resume authorization을
  사용했다. 최종 stop epoch는 `8`, 최소 Actor 여유 `0.5290m`, 정적 여유 `0.3800m`, hard
  failure `0`이다. 35초 종료 시 목표 전에서 마지막 정지를 유지해 임무 완료로 세지 않는다.
- Stress는 READY가 합계 32 tick 있었으나 최대 10개만 연속돼 release·controller call·이동
  모두 `0`이다. 일시 READY나 fresh EMPTY로 출발하지 않았다.
- 검증: R5-C·R2-B 경계 `10 passed`, 관측·예측·gate `66 passed`, 기존 R5-B 영향권
  `28 passed`, 합계 `104 passed`, 실패 `0`. 전체 회귀는 4개 process shard 합계
  `926 passed`, 실패·건너뜀 `0`이다. Ruff·compileall·`git diff --check`도 통과했다.
- 상세:
  `docs/research/dynamic-actor-experiment/20-r2b-appearance-and-observation-recovery.md`
  `docs/research/dynamic-actor-experiment/r5c-observation-recovery-result-2026-08-16.md`
- 다음: 횡단 PASS 복구 reference 계약과 R2-B entry·visibility·coverage 계약은 별도 후속이다.
  formal R5-C/R6, receipt, hidden, 제품 알고리즘·G1~G5·제품 경로분석 7단계는 시작하지 않는다.

## 최신 상태 — R5-C 공개 Normal·Stress 제한 진단 완료, 임무 완료 0

- 작업일: `2026-08-16`
- 기존 문서상 formal R5-C는 R2-B hard failure 2건 해결 전 차단되어 있다. 이번 작업은 사용자
  지시에 따라 Actor가 `t=0`부터 존재하는 횡단 좌·우와 다중 위험 공개 장면만 제한 진단했다.
  R2-B 통과, 카메라·FOV·가림, receipt, hidden 또는 제품 채택을 주장하지 않는다.
- Normal 횡단 LEFT·RIGHT는 모두 tick `80` 새 허가, `81` 첫 움직임, `128` 방향 입력 상실,
  `133` 실제 정지다. 최소 Actor 여유는 각각 `1.8133m`, `1.6643m`, 정적 여유는
  `0.3793m`, `0.3786m`, hard failure `0`이다.
- Normal 다중 위험은 계획 tick `44`에 바로 출발하지 않고 연속 안전 frame 11개를 다시
  확보했다. tick `68` 허가, `69` 첫 움직임, `70` 입력 상실, `73` 실제 정지이며 최소 Actor
  여유 `0.5290m`, hard failure `0`이다.
- Stress 횡단·다중 위험은 방향 예측 `READY`, controller call과 움직임이 모두 `0`이다.
  초기 `stop_epoch=1` 정지를 유지했다. 기준을 낮추거나 안전거리·TTL을 완화하지 않았다.
- 현재 결론은 `안전하게 멈춤, Normal·Stress 임무 완료 0`이다. 입력 상실 뒤 새 stop epoch용
  reference/session·새 허가로 이어가는 recovery는 구현하지 않았다.
- 전용시험 `6 passed`, 관측·방향예측·gate 계약 `66 passed`, 기존 Ideal 횡단·다중 위험
  `8 passed`다. 전체 회귀는 4개 process shard 합계 `922 passed`, 실패·건너뜀 `0`이며
  Ruff·compile·`git diff --check`도 통과했다.
- 상세 문서:
  `docs/research/dynamic-actor-experiment/19-r5c-public-observation-diagnostic.md`
  `docs/research/dynamic-actor-experiment/r5c-public-observation-diagnostic-result-2026-08-16.md`
- 다음 후보는 R2-B 출현/fresh EMPTY 문제와 관측 recovery 명세를 함께 검토하는 것이다.
  formal R5-C/R6, hidden, 제품 알고리즘·G1~G5·제품 경로분석 7단계는 여전히 시작하지 않는다.

## 최신 상태 — R5-B 횡단 좌·우와 다중 위험 공개 Ideal 기능 PASS

- 작업일: `2026-08-16`
- 경로 기능 연구만 진행했다. 제품 알고리즘, G1~G5, 제품 경로분석 7단계, hidden은 건드리지
  않았다.
- 기존 R2-A 횡단 Actor 좌·우를 인과적인 Ideal 합성 관측에 연결했다. 방향성 예측 20 frame이
  준비될 때까지 실제 정지하고 tick `80` 새 허가, tick `81` 첫 움직임 뒤 C++ DWB로 이탈→
  횡단 지점 통과→재합류→도착했다.
- 왼쪽은 `departure 163 → pass 370 → rejoin 610 → goal 625`, 오른쪽은
  `161 → 295 → 541 → 556`이다. 두 방향 모두 최소 Actor 여유 `0.08m` 이상, gate override·
  hard failure `0`, C++ 전체 DWB 코어 사용을 확인했다.
- 곡선에서 안전하게 다음 연속 구간으로 진입했지만 구간 번호가 뒤처지는 문제는 같은 방향의
  바로 다음 구간·기존 경로 오차 `0.05m` 이내·정지/회전 경계 없음일 때만 한 칸 따라잡도록
  수정했다. 허용 오차와 안전거리는 늘리지 않았다.
- 종점 위치에 도달하고 yaw만 남은 경우 공통 executor가 실제 정지 확인→제자리 정렬→종단
  정지·dwell을 수행한다. 마지막 무이동 종단 구간만 남은 DWB GoalAlign 영점 교착도 해당
  경우에 한해 보정했다.
- 다중 위험은 첫 위험 뒤 tick `44` 재개, tick `45` 첫 움직임, `1.7283m` 실제 진행,
  tick `232`의 새 `stop_epoch=2` 재정지, tick `264`의 새 reference/session·새 허가 재개,
  tick `490` 목표 완료를 확인했다. 최소 Actor 여유 `0.1002m`, gate override·hard failure `0`이다.
- 다중 위험의 두 번째 Actor만 `7.0s` 늦춰 휠체어가 실제 접근하는 시각에 맞췄다. 두 Actor는
  모두 `t=0` 지도 밖 진입선부터 같은 속도로 추적되는 Ideal 합성 입력이며, 실제 카메라의
  선행 관측 가능성을 주장하지 않는다.
- 전용 다중 위험 시험 `3 passed`, 빠른 영향권 `79 passed`, 최종 전체 실험실 회귀
  `916 passed`, 실패·건너뜀 `0`, `1357.18s`다. Ruff와 `git diff --check`도 통과했다.
- 상세 문서:
  `docs/research/dynamic-actor-experiment/18-r5b-crossing-and-restop-controller-execution.md`
  `docs/research/dynamic-actor-experiment/r5b-crossing-and-restop-controller-result-2026-08-16.md`
- 남은 범위: Normal·Stress, R2-B Actor 출현/카메라·FOV·가림, 50ms 종단 자격, receipt,
  hidden, 실제 센서·실물·사람 탑승, 제품 알고리즘 채택·G1~G5·제품 경로분석 7단계.

## 최신 상태 — R5-B v2 C++ DWB 공개 Ideal 좌·우 10개 기능 PASS

- 작업일: `2026-08-16`
- 기존 C++ DWB는 공개 10개 모두 추월했지만 `4/10`만 도착했다. 더 빠른 Actor가 있는 여섯
  사례에서 우회 종점 앞 조기 감속과 복귀 회전 중 재추격이 발생했고, 공통 실행 허가가
  `R5-B target is no longer conservatively behind the robot`으로 정상 정지시켰다.
- DWB의 BYPASS 점수 경로에만 종점 앞 `0.30m` 가상 앞보기 점을 추가하고, DWB 실행기의
  BYPASS 완료 오차만 `0.02m`로 제한했다. 실제 reference·안전거리·Actor tube·후보 `217`개·
  후보당 `41` pose·rollout·terminal stopping·critic 가중치·shared gate는 변경하지 않았다.
- 정식 소스와 C++ 전체 DWB 코어로 same-direction 공개 5개 × 좌·우 2개를 재실행했다.
  `10/10` 모두 추월→재합류→도착했고 gate override·hard failure는 `0`이다.
- 추월 tick 범위 `459~478`, 재합류 `757~767`, 도착 `775~785`다. 네 worker 병렬 실행의 총
  wall-clock은 약 `291.71s`다.
- 실행기·DWB adapter·R5-B 직접 영향권은 `35 passed in 269.34s`다.
- 전체 실험실 회귀는 `907 passed in 411.38s`다. Ruff·compileall·diff check도 통과했다.
- 같은 공개 10개 RPP 비교는 `4/10` 도착이다. 순번 0·3 좌·우만 완료했고, 나머지는
  post-pass completion 허가가 없어 tick `604`에서 fail-closed 정지했다. gate override는 `0`이다.
- 결과 문서:
  `docs/research/dynamic-actor-experiment/r5b-v2-cpp-dwb-public-10case-result-2026-08-16.md`
  `docs/research/dynamic-actor-experiment/r5b-v2-rpp-public-10case-comparison-2026-08-16.md`
- 아직 미완료: Normal·Stress, 50ms 무실패 자격, receipt, hidden.
  제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계도 미수행이다.

## 이전 상태 — R5-B v2 C++ 전체 DWB 수치 코어 이식·첫 LEFT 기능 보존 PASS

- 작업일: `2026-08-15`
- 공개 연구용 reference를 controller-matched `0.20m/s`, 최소 측면 offset `0.65m`, 종방향
  completion buffer `0.20m`로 다시 합성했다. 기존 R2와 `0.30m/s` 증거는 회귀 기준으로 보존했다.
- 로봇 뒤 외곽이 현재 directional Actor capsule 앞 외곽보다 기존 minimum clearance 이상 앞선
  경우에만 `POST_PASS_COMPLETION`을 발행한다. 그 뒤 같은 source fresh empty에서만 재합류·도착을
  계속하며 no-frame·stale·invalid·Actor 진행 회귀는 계속 정지로 닫는다.
- 첫 LEFT RPP는 추월 `566`, 재합류 `788`, 완료 `806` tick이다. 마지막 Actor 정합 진행 차이는
  `0.591413977186829m`, gate override·hard failure는 `0`이다.
- 첫 LEFT source-derived DWB+C++ safety batch는 추월 `459`, 재합류 `779`, 완료 `797` tick이다.
  마지막 진행 차이 `0.578492559223498m`, 최소 Actor clearance `0.2863172779913767m`, gate
  override·hard failure `0`이다. 독립 900-tick 회귀는 `1 passed in 160.18s`다.
- 영향권 `52 passed`, 전체 74개 테스트 파일은 4개 process shard에서 합계 `897 passed`,
  failure·skip `0`이다. Ruff·compileall·diff check도 통과했다.
- DWB forward section은 차량 프로필의 기존 최대 전진속도 `0.30m/s`까지 사용한다. 저수준
  generic DWB 기본 `0.20m/s`는 보존했고, 후보 217개·41 pose·rollout·terminal stopping·critic·
  안전 기준·shared gate는 바꾸지 않았다.
- 후보 217개 생성, 후보당 41 pose 적분, 7개 critic 점수·short-circuit·tie·선택과 Manhattan
  거리 지도를 C++20으로 옮겼다. 기존 별도 C++ 동적 안전 배치와 Python shared gate는 유지한다.
- 변경 뒤 첫 LEFT 900틱은 같은 `459→779→797`, gate override·hard failure `0`을 유지하며
  `108.37s`에 완료됐다. 전용 `5 passed`, 직접 영향권 `84 passed`다.
- 전체 75개 테스트 파일은 4개 process shard에서 합계 `905 passed`, failure·skip `0`이다.
- 결과 문서:
  `docs/research/dynamic-actor-experiment/r5b-v2-first-left-controller-matched-result-2026-08-15.md`
  `docs/research/dynamic-actor-experiment/r5b-v2-cpp-full-dwb-port-result-2026-08-15.md`
- 아직 미완료: 좌·우 공개 10-case qualification, Normal·Stress, 50ms 자격, receipt, hidden.
  제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계도 미수행이다.

## 이전 상태 — R5-B Ideal temporal tracking 1차 공개 실행 FAIL

- 작업일: `2026-08-15`
- R2 완료 증거 ZIP의 크기·SHA-256·entry·world/witness/validation provenance를 엄격히
  확인해 same-direction 공개 PASS 5 episode × 좌·우 `10`개를 복원했다.
- 과거 경로를 단순히 2초 이동하는 방식은 Actor clearance를 깨므로 사용하지 않았다. 동결된
  측면 offset 축에서 `tick 40 / 2.0s` 현재 상태의 첫 안전 후보를 다시 찾아 strict
  ground-truth와 R4 공간 validator를 각각 `10/10` 통과했다.
- `GROUND_TRUTH_TEMPORAL` reference, current observation·directional prediction·reference·
  stop epoch·tick을 묶는 일회성 실행 허가, Ideal causal public executor를 구현했다.
- release 전 controller call·motion은 `0`, 최초 controller tick은 `40`, 최초 실제 이동은
  `44`, shared gate override는 `0`이다. fresh-empty·stale·not-ready에서는 이전 허가를
  연장하지 않는다.
- RPP 공개 좌·우 `10/10`은 안전 위반 없이 이탈했지만 Actor 마지막 정합 tick `599`까지
  완전 추월에 필요한 `+0.400m` 분리를 만들지 못했다. 진행 차이는
  `-0.6207526186m ~ +0.1853373303m`이고 모두 ordered overtake·sustained rejoin 없음이다.
- 첫 LEFT DWB도 `564` controller call 뒤 마지막 진행 차이 `-0.08294597353371147m`,
  최대 이탈 `1.1052723922549408m`, 최소 Actor clearance `0.6471500000000001m`, 최소
  static clearance `0.31577553509462813m`, gate override `0`으로 추월·재합류 없이 끝났다.
- DWB wall-clock은 순수 Python에서 `3,074.4671572s`(약 51분)였다. 후보별 dynamic safety만
  C++20 배치 코어로 옮긴 같은 610틱 실행은 `109.597374s`로 약 `28.05배` 빨라졌다.
  최종 pose·clearance·실패·trace hash는 Python 결과와 정확히 같았다. 후보 217개·41 pose·
  안전 기준·critic·tie-break·최종 Python safety 재검사·shared gate는 유지했다.
- C++ 가속 뒤에도 Actor 존재 중 추월·재합류는 없으므로 R5-B는 계속 `FAIL·receipt 0`이다.
  이는 기능 수정이나 제품 DWB 채택이 아니며, 50ms 자격도 이번 lane에는 적용하지 않았다.
- native를 실제 로드한 최종 전체 회귀는 `892 passed`, failure·skip `0`이다. Ruff·compileall·
  diff check와 C++ core 재빌드도 통과했다.
- R5-B·직접 영향권은 `95 passed`, 테스트 파일 73개 전체 회귀는 4개 process shard에서
  `884 passed, 3 skipped`로 끝났다. skip은 선택적 C++ DWA core 미빌드 3건이다.
  Ruff·compileall·diff check도 통과했다.
- 현재 결론은 `R5-B FAIL`, qualification receipt `0`이다. 경로 없음이나 안전 위반이 아니라,
  `0.30m/s` timing으로 검증된 witness를 시간 정보가 없는 geometry reference로 바꾼 뒤
  `0.20m/s` RPP/DWB가 Actor active interval 안에 기동을 끝내지 못한 공통 결합 문제다.
- 상세 결과:
  `docs/research/dynamic-actor-experiment/r5b-initial-public-temporal-tracking-result-2026-08-15.md`
- C++ 가속 상세 결과:
  `docs/research/dynamic-actor-experiment/r5b-cpp-dwb-safety-acceleration-result-2026-08-15.md`
- 새 PC에서는 `simulation/path_planning_lab[native]` 선택 의존성을 설치하고
  `simulation/path_planning_lab/scripts/build_cpp_dwb_safety_core.py`를 실행한다. 생성 DLL은
  Git 비추적 산출물이며 없으면 기존 Python DWB로 fallback한다.
- 다음 공개 연구는 안전 margin·Actor 반경·latency·shared gate·Actor active interval을
  완화하지 않고, reference에 명시적 speed/time profile을 둘지 현재 controller 속도에 맞춰
  causal witness를 다시 합성할지 비교하는 것이다.
- hidden, R5-C/R6/R7, 제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계는 미수행이다.

## 최신 상태 — R5-A v3 공개 static tracking qualification 완료

- 작업일: `2026-08-15`
- commit `7ef755d`의 clean public 21-case는 wide·vertical 6개 완료, crossing 2개와 잘못된
  tick 단위 mirror 관계 판정 때문에 hard `FAIL`·receipt 미생성으로 끝났다.
- stopped `NONE` connector를 직접 구동하지 않으면서, 앞 translation 끝과 connector 끝에
  기존 `0.05m` tolerance를 각각 적용하도록 완료 판정을 보정했다.
- connector 때문에 짧아진 마지막 forward section에서만 DWB exact-score tie가 큰 전진
  후보부터 평가되며, heading 정렬 뒤 GoalAlign·PathAlign near-goal 비활성을 복원했다.
- 좌측 crossing DWB는 `667 tick`, 우측은 `655 tick`에 완료했고 양쪽 모두 deadlock·gate
  override·hard failure `0`이다.
- 관계 audit는 status·section sequence exact 비교를 유지한다. 좌우 signed mirror는 한쪽이
  전진하고 다른 쪽이 같은 chassis yaw로 후진하므로 section별 중심 geometry만 기존 `0.05m`로
  비교하고, travel direction을 보존하는 수평·수직 rigid relation은 footprint axis도 기존
  `0.08rad`로 비교한다.
- 영향권 `66 passed`, 전체 회귀 `864 passed, 3 skipped`, Ruff·compileall·diff check 통과.
- commit `5400000`의 clean 21-case는 ready 8개 RPP·DWB 종단, non-ready 무호출,
  serial/process parity, repeat determinism과 개별 hard safety를 모두 통과했다. 다만 좌우 signed
  relation에서 chassis yaw까지 mirror한 잘못된 판정 4건으로 receipt 없이 닫혔다.
- 관계 의미를 위와 같이 분리한 commit `7810432`의 새 clean 21-case에서 ready 8개 RPP·DWB가
  모두 종단 완료했고, non-ready 13개 controller call `0`, hard·relation failure `0`, deadlock·
  gate override `0`, serial/process parity·repeat determinism `PASS`로 receipt가 생성됐다.
- audit semantic hash는 `aef2ff56060762bab95198345c469f551aa05732891038e05b84c7d940e756e0`,
  receipt content hash는 `b6fcdfbed362bf887200dff817377ebe22ea91ae87338a9ee34d04038bcc1211`다.
- 전체 증거 84개는
  `simulation/path_planning_lab/outputs/persistent-controller-public-20260815-r5a-v3-7810432.zip`
  (`3,462,064 bytes`, SHA-256 `fe26d4143d98347d78b4416d7586f8c57e8b3d10ed96b3748040691d35486aa7`)
  으로 Git 전달한다.
- 상세 결과:
  `docs/research/dynamic-actor-experiment/r5a-v3-crossing-connector-correction-2026-08-15.md`
- 최종 qualification 결과:
  `docs/research/dynamic-actor-experiment/r5a-v3-public-persistent-controller-qualification-result-2026-08-15.md`
- hidden, 제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계는 미수행이다.

## 이전 상태 — R5 v2 대표 signed 후진 종단 통과

- 작업일: `2026-08-15`
- 공통 executor·RPP·source-derived DWB에 R4 v2 section-bound signed translation을 연결했다.
- RPP 공개 대표 case는 실제 후진을 포함해 `297 tick`에 완료, hard failure와 gate 거부 `0`이다.
- DWB의 짧은 reverse 종점 PathAlign local minimum과 self-near reference cursor 회귀를 최소
  수정했다. `wide-straight-left`는 실제 후진 `24 tick`을 포함해 `379 tick`에 완료했고 hard
  failure·deadlock·gate override/rejection은 모두 `0`이다.
- 같은 대표 case의 RPP도 실제 후진 `12 tick`을 포함해 `297 tick`에 완료했다.
- 전체 회귀는 `859 passed, 3 skipped`, Ruff·compileall·diff check를 통과했다.
- 대표 재실행은 dirty·1-case `PARTIAL_REPORT_ONLY`라 receipt가 없으며, clean full public
  21-case qualification은 아직 실행하지 않았다.
- deadlock threshold·safety margin·R4 reference·critic weight·tie-break는 완화하지 않았다.
- 상세 결과:
  `docs/research/dynamic-actor-experiment/r5a-v2-section-bound-reverse-progress-2026-08-15.md`
- 다음 작업: 현재 구현·문서를 commit·push한 뒤 clean commit에서 R5-A public 21-case를 새 output
  경로로 한 번 실행하고, 모든 자격 통과 시에만 receipt를 만든다.
- hidden, 제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계는 미수행이다.

## 최신 상태 — R4 v2 signed reference clean qualification 완료

- 작업일: `2026-08-14`
- 작업 위치: 집 PC
- 기준: R4/R5 v2 section-bound 제한 후진 명세가 승인된 뒤 R4 v2만 구현
- 구현 범위: `travel_direction=FORWARD|REVERSE|NONE`, 방향별 section 분리, 방향 전환 정지
  표식, 독립 validator, v2 hash·reporting·window version
- `ANCHOR_CONNECTOR`: 방향을 추론하지 않는 `NONE` 비실행 연결이며, 실제 변위가 있으면 양 끝
  정지 표식을 요구
- R4 전용 시험: `69 passed`
- 구현 commit: `33a357dceba1c08d6c16db076878d7241d12fdcc`
- 구현 tree: `d7bf278dd5dcd6af19eacfa62ff7725b63b459f7`
- clean public audit: `21/21 PASS`, ready 8·no-reference 11·inconclusive 1·invalid 1,
  relation failure 0, serial/process parity·repeat determinism PASS, receipt 생성
- R5 v1 영향권: 대부분 통과했으나 RPP 대표 signed reference에서 최대 추적오차
  `0.100510m > 0.100000m`; R5 v2 구현 전 명시적 `xfail`로 보존
- 미구현: R5 v2 signed RPP·DWB·common executor, reverse shared-gate 실행, R5 public 재qualification
- 금지 유지: hidden, 제품 알고리즘 채택, G1~G5, 제품 경로분석 7단계
- 결과 문서: `docs/research/dynamic-actor-experiment/r4v2-public-signed-reference-qualification-result-2026-08-14.md`
- 다음 후보: 사용자가 시작하면 R5 v2 signed RPP·DWB·common executor 구현과 static 공개 재qualification

## 0. 집 PC 추가 작업 인계 — R2-A 횡단 우회·재정지 보완

- 추가 작업일: `2026-08-13`
- 작업 위치: 집 PC
- 구현 커밋: `3855598f8b4d404033a58a0e0e4fba192b354005`
- 구현 커밋 tree: `a19cecefaa26d62e5cb1f26e4e74b0bf52e24f06`
- 원격 상태: 구현 커밋까지 `main`과 `origin/main` 일치
- 기존 R2 완료 증거 ZIP: 수정·재생성하지 않음
- hidden: 미실행
- 제품 알고리즘·`G1~G5`·제품 경로분석 7단계: 미결정·미착수

### 집 PC에서 한 작업

사용자가 승인한
`docs/research/dynamic-actor-experiment/r2a-crossing-and-restop-supplement-2026-08-13.md`
범위에 따라 R2-A 미해결 2건만 구현했다.

1. 같은 방향 추월 `PASS_LEFT/RIGHT`와 횡단 Actor 우회를 분리했다.
2. 새 witness 종류 `CROSSING_BYPASS_LEFT/RIGHT`를 추가했다.
3. Actor의 exact trajectory와 직선 reference로 crossing station과 direct-lane blocking
   interval을 label 없이 계산한다.
4. 좌·우 structured 우회 후보를 생성하고 독립 200 Hz validator가 다음 사건을 검증한다.

```text
실제 이탈
→ Actor가 direct lane을 막는 동안 crossing station 우회 통과
→ 같은 reference로 복귀
→ 0.50초 재합류 확인
```

5. 다중 위험에 대해 `GroundTruthHazardInterval`과 `KinematicStopInterval`을 추가했다.
6. 하나의 긴 정지를 두 번 세지 않고 다음 순서를 검증한다.

```text
첫 위험 정지
→ 첫 위험 해소 후 실제 이동
→ 0.10m 이상 progress
→ 두 번째 위험의 별도 정지
→ 두 번째 위험 해소 후 이동·목표 도착·dwell
```

7. audit reporting이 새 crossing witness와 ordered restop 증거를 읽도록 연결했다.
8. 명세 문서, lab README와 TRACEABILITY를 현재 구현 상태로 갱신했다.

### 표적 결과

`legacy_mechanism-14-d796ebd8ba71`:

- 좌·우 횡단 우회 모두 발견
- 방향별 후보 `72개` 생성, strict validator `6개` 통과
- 좌 bypass 시각 `6.280s`
- 우 bypass 시각 `7.485s`
- 두 방향 모두 우회 뒤 `0.50s` 재합류 확인
- R2-A evaluator 결과 `MATCHED`
- hard failure 없음

`legacy_mechanism-18-28f0a990202f`:

- 첫 정지 `0.00~2.10s`
- 위험 사이 실제 이동 progress `0.3975m`
- 두 번째 별도 정지 `4.70~6.15s`
- 이후 목표 도착·dwell 완료
- 증거 수준 `RESTOP_AND_RECOVERY_PROVEN`
- R2-A evaluator 결과 `MATCHED`
- 기존 R2-B 보류 오류 `ideal_capsule_ground_truth_miss`는 그대로 남음

적대 입력인 “첫 위험부터 두 번째 위험까지 계속 정지”는
`continuous_hold_misclassified_as_restop`과 `second_stop_not_distinct`로 거부됐다.

### 집 PC 검증과 환경 상태

실행한 검증:

- 신규 R2-A 표적 시험 함수 3개 직접 실행: 통과
- legacy 14·18 표적 audit: 두 R2-A 판정 모두 `MATCHED`
- Python `compileall`: 통과
- `git diff --check`: 통과

실행하지 않은 검증:

- 정식 `pytest` 전체 회귀
- Ruff
- 공개 19개 전체 witness audit 재실행

초기 집 환경에 pytest와 Ruff가 없어 전체 시험을 실행하지 않았다. 이후 사용자 허가를 받아
저장소 루트 `.venv`에 프로젝트 개발 의존성을 설치했다.

```text
pip 26.2.1
pytest 9.1.1
pytest-cov 7.1.0
ruff 0.16.2
hospital-path-planning-lab editable install
```

`.venv/`는 `.gitignore` 대상이므로 GitHub로 전달되지 않는다. 회사 PC는 자신의 기존
`.venv`를 사용하거나 `pip install -e ".\simulation\path_planning_lab[dev]"`로 설치한다.

### 회사 PC에서 바로 이어갈 순서

1. GitHub `main`을 fast-forward 동기화한다.
2. 회사 PC의 `AGENTS.md`와 이 문서를 다시 읽는다.
3. 개발 의존성이 준비됐는지 확인한다.
4. 먼저 신규 표적 시험과 기존 witness 관련 집중 회귀를 실행한다.
5. 이어서 전체 pytest와 Ruff를 실행한다.
6. 실패가 있으면 공개 19개 전체 재실행 전에 원인을 수정하고 표적·영향권 시험을 다시 실행한다.
7. 전체 회귀가 통과한 뒤에만 새 output 경로에서 공개 19개 R2-A audit 재실행 여부를 결정한다.
8. R2-A가 안정화된 다음 `ideal_capsule_ground_truth_miss` R2-B 2건을 별도 원인 분석한다.

하지 말 것:

- 기존 R2 ZIP이나 output 덮어쓰기
- hidden 실행
- R2-B 오류를 R2-A 성공으로 숨기기
- 전체 회귀 전에 공개 19개 장시간 재계산
- 제품 알고리즘 채택, `G1~G5` 결정 또는 제품 경로분석 7단계 시작
- 실제 사람·환자 탑승 안전 주장

- 작성일: `2026-08-13`
- 인계 방향: 회사 PC → 집 PC
- 저장소: `https://github.com/shotgun1107/hospital-autonomous-wheelchair.git`
- 정본 브랜치: `main`
- GitHub 계정: `shotgun1107`
- 현재 연구 범위: Python `simulation_only`, 공개 synthetic corpus, 실제 사람 미탑승
- 팀 전체 합의·제품 알고리즘·`G1~G5`·제품 경로분석 7단계: 미결정

## 1. 이 문서의 목적

오늘 회사 PC에서는 R2 ground-truth witness 자동 탐색·공개 감사 결과를 정리하고, 외부 Pro
조사를 받아 경로 연구와 카메라·관측 연구의 gate를 분리했다.

집 PC에서는 GitHub `main`의 최신 상태를 안전하게 동기화한 뒤 다음 질문을 이어서 다룬다.

> 사람의 정확한 위치를 알고 있다고 가정할 때, 휠체어 차체가 허용된 공간에서 실제로
> 통과·회전·재합류할 수 있는가?

바로 다음 작업은 카메라 구현이나 DWB 튜닝이 아니라 `R3 bounded 공간 oracle`의 상세 명세다.

## 2. Git 정본과 자격증명

오늘 인계 문서 작성 직전 정본은 다음과 같다.

```text
branch: main
HEAD/origin-main: 84915a779b924190e54894f5ff97b12e1fd630d2
tree: c945a293c4f0cac63d761979cb1d9704144c5c20
worktree: clean
stash: none
local-only branch: codex/legacy-python-dwa-experiment-20260812
local-only commit: 0f9654e9fb54f6630ef8b591061510e23bd49e46
```

이 local-only branch는 과거 Python DWA 실험 보존용이며 `origin/main`의 ancestor가 아니다.
이번 인계 대상도 아니고 원격에 push하지 않았다. 회사 PC에서 삭제·merge·rebase하지 않고
그대로 보존한다. 집 PC에는 GitHub main 동기화로 전달되지 않으며 R3 명세 작업에 필요하지
않다.

이 문서 자체를 커밋·푸시한 최종 동기화 HEAD와 tree는 대화에서 제공되는 집 PC 시작
프롬프트의 `expected HEAD/tree`를 정본으로 사용한다. 다음 주요 commit은 최종 HEAD의
ancestor여야 한다.

| 역할 | commit |
|---|---|
| source-derived DWB 기준선 | `340cb400ca51229fcd7c032b97a98369c757586f` |
| R2 witness 계약·독립 validator | `435490b` |
| R2 WAIT/HOLD search | `d2807b6` |
| R2-PASS structured search | `2b1cec9` |
| R2 profile replay | `5569cf4` |
| R2 공개 13+6 audit runner | `4e4ba0fb91d67498fe163aca99ff1ab647224f08` |
| R2 공개 감사 결과 문서 | `b2e182cc57d9e0dc4fc335b3f03f2d9fcfe3c235` |
| R2-A/R2-B gate 분리 | `84915a779b924190e54894f5ff97b12e1fd630d2` |

회사 PC에서는 `main`과 `origin/main`이 일치하고 작업트리가 clean한 상태에서 인계 문서를
작성했다. 인계 문서 커밋 뒤에도 `main`에 직접 push한다.

### Git 인증

Windows Git Credential Manager의 HTTPS 자격증명을 사용한다. 토큰·비밀번호를 문서나
채팅에 직접 쓰지 않는다.

```powershell
git ls-remote origin refs/heads/main
```

이 명령이 성공하면 `gh auth status`가 실패해도 Git 동기화는 가능하다. Git 원격 접근까지
실패할 때만 다음으로 `shotgun1107` 계정을 브라우저 인증한다.

```powershell
gh auth login -h github.com
```

## 3. 과거 — 왜 R2를 만들었는가

기존 사용자 정의 DWA는 공개 시험에서 실질적인 우회·재합류를 만들지 못했다. 그러나 그
결과에는 다음이 섞여 있었다.

- 직선 reference만 받은 controller가 좌·우 통과 topology를 만들지 못함
- 후보 탈락 원인과 기능 oracle 부족
- 수작업 feasible witness와 실제 controller action primitive 차이
- Python 실행속도와 알고리즘 기능 판정 혼합
- 방향 없는 원형 Actor reachable tube의 과팽창
- 공개 기능 확인 전 hidden으로 넘어가던 과거 runner gate

따라서 “DWA가 나쁘다”가 아니라 다음 순서로 원인을 분리하기로 했다.

```text
정답 경로가 존재하는가
→ 공간적으로 차체가 지나갈 수 있는가
→ controller용 reference로 표현할 수 있는가
→ 같은 reference에서 controller를 비교
→ 연속 종단시험
→ 마지막에만 C++ timing·hidden gate
```

이를 `R1~R7` 연구 단계로 정리했다.

## 4. 오늘 완료한 구현과 실행

### 4.1 R2 WAIT/HOLD·PASS 자동 탐색

R2 search는 controller·category·oracle·hidden을 입력으로 받지 않고 공개 episode에서 다음
witness를 찾는다.

```text
WAIT_AND_FOLLOW
HOLD_ONLY
PASS_LEFT
PASS_RIGHT
```

검증 조건:

- 20Hz 가상 차체 운동학과 선·각가감속
- 200Hz exact oriented footprint evaluator
- static obstacle·forbidden zone
- exact Actor circle surface clearance `0.08m`
- terminal stopping
- ordered departure→overtake→rejoin
- wait 뒤 실제 progress
- episode 전체 HOLD
- partial/resource/no-witness 구분

같은 방향 wide 공개 5개의 PASS 완전탐색 결과:

```text
generated:        135,360
geometry reject:   26,382
dynamic reject:    70,318
validated:         38,660
```

Python 완전탐색은 process 14개, shard 2,048로 병렬화했다. 약 29분이 걸렸지만 이는 운영
진단이며 경로 가능성·안전·알고리즘 자격에 사용하지 않는다. 실제 연산 자격은 R7의 동결
C++ 구현에서만 측정한다.

### 4.2 R2 공개 13+6 audit runner

다음이 구현됐다.

- v6 공개 13개 + legacy mechanism golden 6개, 합계 19개
- WAIT/HOLD·PASS search
- 독립 ground-truth validation
- Ideal·Normal·Stress profile replay
- JSON·Markdown·PNG
- partial/complete lifecycle와 completion receipt
- 기존 output 비덮어쓰기

완료 실행:

```text
source commit: 4e4ba0fb91d67498fe163aca99ff1ab647224f08
source tree:   b5df045fcf7f03269cf3f6056c93bb9a2b091432
episodes:      19/19 complete
expected assessment: 17 matched / 1 mismatched / 1 not fully covered
hard failures: 2
hidden: false
```

전체 ground-truth path search 자체에서 확인한 내용:

- WAIT/HOLD witness 검증
- same-direction-wide 5개의 좌·우 PASS 검증
- selected witness의 collision·forbidden·clearance hard failure 없음
- legacy 횡단 Actor detour 1건은 PASS-v1 범위 밖 `SEARCH_INCONCLUSIVE`
- legacy dynamic-change 1건은 두 위험의 재정지 순서 `not fully covered`

### 4.3 전체 회귀

R2 runner 구현 뒤 회사 PC에서 다음을 확인했다.

```text
pytest: 688 passed in 587.64s
Ruff: passed
compileall: passed
git diff --check: passed
```

이후 변경은 문서뿐이며 코드·시험 의미를 바꾸지 않았다.

## 5. 오늘 발생한 문제와 해결·우회

### 5.1 첫 full 실행 tool timeout

첫 실행은 외부 도구 timeout 약 10초로 중단됐다.

처리:

- 산출물을 삭제하거나 덮어쓰지 않음
- `run_state.incomplete.json`과 manifest 보존
- `INFRASTRUCTURE_INCOMPLETE`로 분류
- 알고리즘 실패·NO_WITNESS로 사용하지 않음
- 새 output 경로에서 전체 실행 재시작

### 5.2 Python search가 오래 걸림

원인:

- PASS 후보 135,360개
- 후보마다 운동학·terminal stopping·200Hz 검증

처리:

- episode/candidate shard를 process 기반 병렬화
- 각 shard 내부 순서와 global ordinal 보존
- deterministic merge와 parent strict 재검증
- worker·shard·wall-clock은 semantic hash에서 제외
- timing 합격조건 자체를 Python R2에서 제거하고 R7 native로 이동

후보 수·안전조건·episode·평가기준은 줄이지 않았다.

### 5.3 R2 hard failure 2건

실패 장면:

- v6 second-risk
- legacy dynamic-change

공통 원인:

```text
Actor가 episode 중간에 지도 내부에 즉시 생성
+ Functional Ideal에도 100ms latency
+ 최신 관측은 생성 전 fresh EMPTY
+ prediction shape 0개
+ 실제 Actor containment miss
```

이 실패는 경로 충돌, DWA, Python 속도나 Capsule 반경 문제가 아니다. 관측·Actor 출현 계약
문제다. 기존 R1은 생성된 shape에서 Actor 방향으로만 검사해 `Actor 있음 + shape 없음`을
놓쳤고, R2 reverse audit가 이를 발견했다.

### 5.4 Pro 조사와 범위 충돌

회사 PC에서 문제보고서·관련 코드·실패 원본을 ZIP으로 만들고 Pro 조사를 받았다. Pro의
다음 지적은 승인했다.

- fresh EMPTY 단독 비영점 이동 허용은 online false-safe
- 지도 내부 순간 생성과 latency는 기존 Ideal containment와 동시에 만족 불가
- R1 total Actor coverage blind spot
- 기존 hard failure·receipt 보존 필요

그러나 Pro는 관측 계약을 고치기 전 R3도 금지해야 한다고 판정했다. 사용자는 카메라는 후속,
경로를 먼저 확인하기로 결정했다. R3는 observation·prediction을 입력으로 받지 않는 static
공간 oracle이므로 Pro gate를 그대로 적용하지 않았다.

해결:

```text
R2-A = exact ground-truth 시간 경로 연구
R2-B = 관측·prediction·후속 카메라 판단 가능성
```

- R2-A partial 결과와 미해결 공간 사례는 R3로 전달 가능
- R2-B hard failure 2건은 삭제하지 않고 후속 보류
- R2-B 통과 전 perception-integrated R6·hidden·제품 안전 주장 금지

기준 문서:

- `docs/decisions/0011-separate-path-and-perception-research-gates.md`
- `docs/reviews/pro-r2-actor-appearance-review-disposition-2026-08-13.md`

## 6. 현재 정확한 단계 판정

| 단계 | 현재 상태 | 의미 |
|---|---|---|
| `R1` | narrow claim 완료 | 생성된 prediction shape의 연결 Actor 포함; total Actor coverage는 후속 |
| `R2-A` | 부분 완료 | WAIT/HOLD·same-direction PASS 검증, 횡단·다중 위험 미완료 |
| `R2-B` | 실패·후속 보류 | Actor 출현 coverage hard failure 2건, 카메라 나중에 처리 |
| `R3` | 미시작, 명세 진입 가능 | static 공간에서 차체 통과·회전·재합류 가능성 |
| `R4` | 미시작 | WAIT/LEFT/RIGHT local reference 계약 |
| `R5` | 미시작 | persistent RPP·DWB 동일 reference 비교 |
| `R6` | 미시작 | 연속 공개 종단 자격 |
| `R7` | 미시작 | C++ semantic parity·timing·hidden gate |

현재 R2-A 문제:

1. PASS-v1이 직선·같은 방향 Actor 전용이라 횡단 detour를 표현하지 못함
2. 다중 위험의 stop→resume→restop 전체 순서 미증명
3. HOLD 존재를 임무 성공으로 볼 수 없음
4. structured search 실패와 실제 공간 불가를 구분하지 못함

4번을 독립적으로 분해하는 것이 R3다.

## 7. R2 완료 산출물의 Git 동기화

`simulation/path_planning_lab/outputs/`는 기본적으로 `.gitignore` 대상이다. 다만 135,360개
PASS 후보를 집 PC에서 다시 계산하지 않고 선택 경로와 완료 증거를 인수할 수 있도록 최종
완료 output 전체의 압축본 하나만 Git에 명시적으로 포함했다.

### R2 실행 산출물

```text
outputs/witness-audit-public-20260813-r2-v1-4e4ba0f/
  최초 incomplete 실행

outputs/witness-audit-public-20260813-r2-v2-4e4ba0f/
  최종 19/19 complete 실행

outputs/witness-audit-public-20260813-r2-v2-4e4ba0f.zip
  Git으로 전달되는 최종 complete 압축 증거
```

최종 실행 hash:

```text
manifest: bd25b0348fd729c3df7aba49bc3c9c5ef50dfcbf6386b7197a0aab6de94f495f
semantic: 13448a876803d2d0631a549a4e6c9c451c76fc7571a6c0e0be820ef7a14ee9d0
report:   2aec4da6d96967ef3ca24b910c01b8ca31aeae18a52dc3162202aa7f0e0ab431

ZIP size:    3,657,108 bytes (3.488 MiB)
ZIP SHA-256: 50567b093082a57232e668ef89c0316a426cd936496e465b943fe57efa894266
```

ZIP에는 19개 episode의 선택 witness 좌표, 좌·우 PASS 선택 결과, 검증·profile replay,
manifest, 완료 receipt, JSON·Markdown·PNG가 들어 있다. 135,360개 탈락 후보의 전체 좌표는
streaming 평가 뒤 저장하지 않았으므로 포함되지 않는다. 집 PC에서는 현재 동결 실행의 선택
경로와 결과를 확인하기 위해 135,360개를 다시 계산할 필요가 없다. 코드·안전 계약·corpus가
변경되거나 탈락 후보를 새로운 기준으로 재분석할 때만 새 output 경로에서 재실행한다.

### Pro 조사 ZIP

```text
outputs/pro-investigation-packets/
  hospital-autonomous-wheelchair-r2-actor-appearance-pro-investigation-2026-08-13.zip
```

```text
size: 568,043 bytes
SHA-256: 3c33626cbddd60e50c105d42781ed827b8eab383e43de8b0947f3be8e6960118
```

최종 complete ZIP은 Git pull로 집 PC에 전달된다. 압축을 풀어 원본 JSON·PNG와 선택 경로를
확인할 수 있으며, 기존 output 경로가 이미 존재하면 덮어쓰지 않는다. 최초 incomplete v1과
Pro 조사 ZIP은 계속 회사 PC 로컬에만 보존한다.

## 8. 집 PC 안전 동기화 절차

### 8.1 AGENTS와 로컬 상태 확인

```powershell
Set-Location '<집 PC 저장소 경로>'
Get-Content -LiteralPath AGENTS.md -Raw -Encoding UTF8
git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes
```

상위 경로에 다른 `AGENTS.md`가 있으면 함께 읽는다.

### 8.2 로컬 변경 보존

집 PC 변경을 삭제하거나 덮어쓰지 않는다.

1. modified·staged·untracked 파일명을 확인한다.
2. `.env`, token, key, credential 등 민감 파일은 stage하지 않고 즉시 보고한다.
3. 안전한 변경이면 원격에 push하지 않는 backup 브랜치를 만든다.
4. backup commit으로 보존한다.

예시:

```powershell
git switch -c backup/home-before-r2-r3-sync-20260813
git add -A
git commit -m "backup home work before R2-R3 sync"
```

금지:

- `git reset --hard`
- `git clean`
- `git restore <file>`
- `git checkout -- <file>`
- 무단 stash
- rebase
- force push

### 8.3 main fast-forward

```powershell
git ls-remote origin refs/heads/main
git fetch origin --prune
git switch main
git pull --ff-only origin main
```

fast-forward가 실패하거나 local main과 origin/main이 갈라지면 reset·rebase·merge하지 말고
다음을 보고한다.

- local main HEAD
- origin/main HEAD
- 양쪽에만 있는 commit
- 보존한 backup branch·commit
- 영향을 받는 파일

### 8.4 동기화 검증

대화에서 제공되는 최종 expected HEAD·tree를 사용한다.

```powershell
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t 4e4ba0fb91d67498fe163aca99ff1ab647224f08
git merge-base --is-ancestor 4e4ba0fb91d67498fe163aca99ff1ab647224f08 HEAD
git cat-file -t 84915a779b924190e54894f5ff97b12e1fd630d2
git merge-base --is-ancestor 84915a779b924190e54894f5ff97b12e1fd630d2 HEAD
git status --porcelain=v1
git diff --exit-code HEAD --
git diff --cached --exit-code HEAD --
```

통과조건:

- HEAD와 origin/main이 final prompt의 expected HEAD와 일치
- tree가 final prompt의 expected tree와 일치
- 두 required object type은 `commit`
- 두 ancestry 명령 exit `0`
- status 출력 없음
- 두 diff 성공

## 9. 집 PC에서 읽을 문서 순서

1. `AGENTS.md`
2. `인수인계.md`
3. `docs/decisions/0011-separate-path-and-perception-research-gates.md`
4. `docs/reviews/pro-r2-actor-appearance-review-disposition-2026-08-13.md`
5. `docs/research/dynamic-actor-experiment/10-dynamic-local-maneuver-research-master-spec.md`
6. `docs/research/dynamic-actor-experiment/11-witness-automation-and-generalization.md`
7. `docs/research/dynamic-actor-experiment/12-pass-structured-witness-search.md`
8. `docs/research/dynamic-actor-experiment/13-witness-profile-replay.md`
9. `docs/research/dynamic-actor-experiment/r2-public-witness-audit-result-2026-08-13.md`
10. `simulation/path_planning_lab/README.md`
11. `simulation/path_planning_lab/TRACEABILITY.md`

## 10. 집 PC에서 바로 이어서 할 작업

### 첫 작업: R3 상세 명세

새 파일:

```text
docs/research/dynamic-actor-experiment/14-bounded-spatial-oracle.md
```

먼저 명세만 작성한다. 구현은 명세 검토 뒤 시작한다.

R3 명세가 답해야 할 질문:

> Actor 시간·관측·controller를 제거한 static 공간에서 oriented wheelchair가 현재 경로에서
> 이탈해 장애물 옆을 통과하고 목표 pose·heading으로 재합류할 수 있는가?

필수 포함:

- 입력: static grid, allowed/forbidden, footprint, start, side, rejoin pose·heading
- bounded search region과 map provenance
- offline State Lattice 또는 동등 pose·heading oracle 역할
- 가상 motion primitive와 `simulation_only` 표시
- 해상도·heading bin·primitive·resource limit 동결 방식
- exact footprint 독립 validator
- `SPATIALLY_FEASIBLE`, `SPATIALLY_INFEASIBLE`, `RESOURCE_LIMIT`, `INVALID_INPUT`
- resource limit을 불가능 판정으로 사용하지 않는 규칙
- path·primitive·clearance·expanded state·hash 출력
- wide/narrow/door/dead-end/corner/vertical/left-right mirror 시험
- R2-A 횡단 Actor scene의 static geometry projection
- Python wall-clock을 판정에서 제외하고 native timing은 R7로 미루는 규칙
- category·oracle·ground truth 누출 금지
- 비덮어쓰기 JSON/PNG와 hidden 금지
- R4 전달 계약과 제품·실물 증거 한계

### 이번 집 세션에서 금지

- 카메라·FOV·가림·사람 검출 구현
- R2-B hard failure 완화 또는 삭제
- safety `0.08m`, Actor radius, corpus 결과 맞춤 변경
- R3 명세 승인 전 State Lattice 구현
- DWB/RPP 튜닝
- C++ 이식·timing qualification
- hidden 생성·조회·실행
- 제품 알고리즘·`G1~G5`·경로분석 7단계 결정

### 불필요한 재실행

동기화 확인을 위해 다음을 다시 돌릴 필요는 없다.

- 32분 R2 공개 full audit
- 135,360 PASS 후보 완전탐색
- ignored 회사 output 재생성

코드 변경이 없는 R3 문서 작업에서는 `git diff --check`, UTF-8·링크 정합성과 관련 문서
충돌만 확인한다. 코드 구현을 시작한 뒤에만 관련 pytest와 전체 회귀를 실행한다.

## 11. 한 줄 인계

```text
R2-A는 WAIT/HOLD·같은 방향 좌우 PASS를 ground truth로 검증했고 횡단·다중 위험이 남았다.
R2-B의 Actor 출현 coverage hard failure 2건은 카메라 후속으로 보존한다. 집 PC에서는 이를
섞지 말고 R3 static 차체 공간 oracle 상세 명세부터 작성한다.
```
