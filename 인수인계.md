# 병원 자율주행 휠체어 프로젝트 인수인계

## 0. 문서 목적

이 문서는 회사 PC에서 진행한 경로 알고리즘 연구 작업을 집 PC와 다음 Codex 작업에
안전하게 이어주기 위한 인수인계다.

다음 내용을 한곳에 정리한다.

- 과거: 저장소 동기화 과정에서 어떤 상황이 있었는가
- 현재: GitHub `main`에 무엇이 들어 있고 무엇이 아직 미구현인가
- 미래: 집 PC를 어떻게 맞추고 다음에 무엇을 구현할 것인가
- 시작 프롬프트: 새 Codex 대화에 그대로 붙여 넣을 작업 지시

이 문서는 프로젝트 제품 결정서가 아니다. 제품 범위와 안전 기준은 `AGENTS.md`와
`docs/product`, `docs/safety`의 기준 문서를 우선한다.

---

# 1. 한눈에 보는 현재 상태

| 항목 | 현재 값 |
|---|---|
| GitHub 저장소 | `https://github.com/shotgun1107/hospital-autonomous-wheelchair.git` |
| GitHub 기본 브랜치 | `main` |
| 현재 회사 PC 브랜치 | `main` |
| 1단계 구현 커밋 | `3f0e1425056514ad0bba77c9102f11afcb406554` |
| 2단계 구현 커밋 | `5aa7fed5c7e4b3243a9e87db9688b563db183237` |
| 로컬·원격 상태 | 회사 PC `main`과 `origin/main` 일치 확인 |
| 완료 단계 | 동적 원형 Actor 비교실험 1·2단계 |
| 다음 후보 단계 | 3단계 safety gate·권한·시간 |
| 증거 범위 | Python `simulation_only` |

현재 구현은 실제 사람, 실제 센서, ROS 2, 모터, 실물 차체 또는 환자 탑승 안전성의
증거가 아니다.

---

# 2. 과거 — 어떤 상황이 있었는가

## 2.1 초기 기준선

GitHub `main`의 초기 기획 기준선은 다음 커밋이었다.

```text
d2fecb7 병원 자율 휠체어 프로젝트 기획 기준선 추가
```

이후 프로젝트 지침과 팀 책임 상태를 복구한 커밋이 만들어졌다.

```text
df24c09 프로젝트 지침과 팀 역할 상태 복구
```

그 위에 Python 경로 알고리즘 실험실과 동적 Actor 계획·설계·1단계 구현이 순서대로
추가됐다.

```text
7832d58 add path-planning experiment lab
a9abf53 document dynamic actor experiment plan
cb8afed add dynamic actor experiment design specs
3f0e142 implement dynamic actor simulation core
```

## 2.2 발생한 문제

GitHub의 기본 브랜치는 계속 `main`이었지만, 작업은
`codex/path-planning-python-lab` 브랜치에서 진행됐다. 작업 브랜치에 commit과 push는
정상적으로 됐지만 완성된 작업을 즉시 `main`에 병합하지 않았다.

원인은 다음과 같다.

- Codex 작업 브랜치를 별도 기능 브랜치로 보수적으로 유지했다.
- `main` 병합을 별도 승인 작업으로 간주했다.
- 사용자가 이미 `main`을 기본 기준선으로 지정했다는 맥락을 충분히 반영하지 못했다.

데이터 유실이나 강제 덮어쓰기는 없었다. 문제는 작업 결과가 기능 브랜치에만 있고
GitHub 기본 브랜치에 반영되지 않았던 것이다.

## 2.3 수정한 방법

다음 순서로 안전하게 확인하고 병합했다.

1. 회사 PC 작업트리가 깨끗한지 확인했다.
2. `git fetch origin --prune`으로 원격 상태를 다시 받았다.
3. `origin/main`이 작업 브랜치의 조상인지 `merge-base`로 확인했다.
4. 충돌 없는 fast-forward가 가능함을 확인했다.
5. 로컬 `main`으로 전환했다.
6. `git merge --ff-only codex/path-planning-python-lab`을 실행했다.
7. `git push origin main`으로 GitHub `main`을 갱신했다.
8. 로컬 HEAD, `origin/main`, GitHub 기본 브랜치가 모두 일치하는지 확인했다.

1단계 기능 병합 직후 결과:

```text
main = origin/main = 3f0e142
GitHub default branch = main
```

이 인수인계 문서가 추가된 뒤의 최신 `main` hash는 고정 문자열로 추측하지 않고
`git rev-parse origin/main`으로 확인한다. `3f0e142`는 1단계 기능 자체의 기준 커밋이다.

다음 작업은 수행하지 않았다.

- force push
- `git reset --hard`
- 사용자 로컬 파일 삭제
- 기존 작업 브랜치 삭제
- 생성 output을 Git에 억지로 추가

`codex/path-planning-python-lab` 브랜치는 동일 커밋을 가리키는 기록용 브랜치로 남아 있다.

---

# 3. 현재 — 무엇이 구현됐는가

## 3.1 경로 알고리즘 실험실

`simulation/path_planning_lab`에 다음 연구용 구성요소가 들어 있다.

- Dijkstra, A*, D* Lite
- Grid A*
- DWA
- Pure Pursuit, RPP
- graph/grid 지도 생성과 golden/development/hidden/regression 구조
- collision·forbidden·revision·provenance 검증
- JSON·Markdown·PNG 실험 산출물

전체 실험실 회귀시험은 동적 Actor 1·2단계 포함 상태에서 통과했다.

```text
197 passed in 193.43s
```

## 3.2 동적 Actor 전체 계획

요구사항 동결본:

- [움직이는 원형 Actor 회피 비교실험 v5](docs/research/dynamic-person-avoidance-experiment-plan-2026-08-10.md)

전체 구조와 단계별 설계:

- [동적 원형 Actor 비교실험 설계 명세](docs/research/dynamic-actor-experiment/README.md)

단계는 다음과 같다.

1. 동적 시뮬레이션 기반
2. 관측 생성과 Actor 예측
3. safety gate·권한·시간
4. PP·DWA closed-loop 통합
5. ground-truth 평가기와 corpus
6. runner·hidden·통계·보고

## 3.3 완료된 1단계와 2단계

명세:

- [1단계 동적 시뮬레이션 기반](docs/research/dynamic-actor-experiment/01-dynamic-simulation-core.md)

구현:

- `simulation/path_planning_lab/src/hospital_path_lab/dynamic_contracts.py`
- `simulation/path_planning_lab/src/hospital_path_lab/dynamic_actor.py`
- `simulation/path_planning_lab/src/hospital_path_lab/simulation.py`
- `simulation/path_planning_lab/src/hospital_path_lab/experiment_visualization.py`

시험:

- `simulation/path_planning_lab/tests/test_dynamic_actor.py`
- `simulation/path_planning_lab/tests/test_dynamic_simulation.py`

구현 내용:

- 반지름 `0.18m`의 open-loop 원형 Actor
- 최대 Actor 속도 `0.50m/s`
- seed 기반 복도 횡단 waypoint 생성
- 20Hz, 6.5초, ground-truth 131 frame
- `tick_id × 0.05s` 기반 결정론적 simulation time
- Stage 1에서 정지한 로봇과 0 command 130개
- controller-facing frame에서 Actor ground truth와 seed 제외
- 같은 seed에서 동일 scenario·trace·SHA-256 생성
- episode ID와 seed를 포함한 JSON·PNG 출력

고정 seed `20260810`의 trace hash:

```text
88fd181c70b3ea08d19d7403a391244b57ce69e4f317c18f9c7e7b58c141db81
```

2단계 명세:

- [2단계 관측 생성과 Actor 예측](docs/research/dynamic-actor-experiment/02-observation-and-prediction.md)

2단계 구현·시험:

- `simulation/path_planning_lab/src/hospital_path_lab/dynamic_observation.py`
- `simulation/path_planning_lab/src/hospital_path_lab/dynamic_prediction.py`
- `simulation/path_planning_lab/tests/test_dynamic_observation.py`
- `simulation/path_planning_lab/tests/test_dynamic_prediction.py`

구현 내용:

- 20 Hz ground truth를 외부에서 소비하는 10 Hz 관측 stream
- Normal·Stress·Boundary 300/350 ms 동결 profile
- 위치·속도 Gaussian noise, frame별 독립 dropout과 별도 4-frame burst
- fresh `EMPTY`와 no-frame 구분
- source·episode·map·sequence·revision·hash·binding·σ·시간 순서 검증
- `age == 300ms` fresh, 초과 stale 판정
- validator의 fresh snapshot만 받는 controller 비종속 Actor prediction tube
- 벡터 크기 `0.50m/s` clamp, 50 ms 적용 지연, 2σ와 가속 reachable bound
- 관측·예측 전용 `43 passed`, 1·2단계 동적 영향권 `53 passed`

## 3.4 Git에 들어가지 않는 자료

다음 경로는 `.gitignore` 대상이다.

```text
simulation/path_planning_lab/outputs/
```

따라서 회사 PC에서 만든 다음 JSON·PNG는 GitHub나 집 PC로 자동 전송되지 않는다.

```text
outputs/stage1_dynamic_actor/corridor_crossing_v1_seed_20260810.json
outputs/stage1_dynamic_actor/corridor_crossing_v1_seed_20260810.png
```

집 PC에서는 코드 동기화 후 필요하면 같은 seed로 다시 생성해야 한다. 같은 Python 환경과
동결 코드를 사용하면 JSON 의미 내용과 trace hash가 같아야 한다.

## 3.5 아직 구현하지 않은 것

- 동적 safety gate와 `stop_epoch`
- PP·DWA 동적 closed loop
- 200Hz ground-truth evaluator
- 동적 development·hidden corpus와 통계 판정

현재 정적 DWA와 기존 synthetic dynamic-local 증거가 존재하더라도 위 v5 동적 사람 비교
계약이 구현됐다는 뜻은 아니다.

---

# 4. 집 PC 동기화

## 4.0 집 PC에 저장소가 아직 없는 경우

저장소를 둘 상위 디렉터리에서 clone한다.

```powershell
git clone https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
Set-Location hospital-autonomous-wheelchair
git switch main
git status -sb
```

이미 저장소가 있으면 다시 clone하지 말고 아래 상태 확인부터 진행한다.

## 4.1 가장 먼저 확인할 것

집 PC에서 바로 pull하지 말고 먼저 저장소와 변경 상태를 확인한다.

```powershell
Set-Location '집 PC의 hospital-autonomous-wheelchair 저장소 경로'
git status -sb
git status --short
git remote -v
git branch -vv
```

원격 `origin`은 다음 저장소여야 한다.

```text
https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
```

## 4.2 집 PC가 깨끗한 경우

`git status --short` 출력이 없으면 다음 순서로 동기화한다.

```powershell
git fetch origin --prune
git switch main
git pull --ff-only origin main
git status -sb
git rev-parse HEAD
git rev-parse origin/main
```

마지막 두 hash는 같아야 한다. `git status -sb`는 다음처럼 보여야 한다.

```text
## main...origin/main
```

## 4.3 집 PC에 수정 파일이 있는 경우

수정 파일을 삭제하거나 덮어쓰지 않는다. 먼저 backup 브랜치에 보존한다.

```powershell
git switch -c backup/home-before-main-sync
git add -A
git commit -m "backup home local work before main sync"
git fetch origin --prune
```

그다음 `main`을 확인한다.

```powershell
git switch main
git pull --ff-only origin main
```

fast-forward가 성공하면 끝이다. 실패하면서 `divergent branches`가 나오면 집 PC `main`에
GitHub에 없는 로컬 commit이 있다는 뜻이다. 이미 backup 브랜치가 그 commit과 파일을
보존하고 있는지 다음으로 확인한다.

```powershell
git log --oneline --decorate backup/home-before-main-sync -5
git status --short
```

보존이 확인된 뒤에만 backup 브랜치에서 로컬 `main` 포인터를 원격에 맞춘다.

```powershell
git switch backup/home-before-main-sync
git branch -f main origin/main
git switch main
git status -sb
```

이 절차는 `reset --hard`를 사용하지 않으며 이전 집 PC 상태는 backup 브랜치에 남긴다.
backup 브랜치 이름이 이미 있으면 날짜나 숫자 suffix를 붙인다.

## 4.4 동기화 검증

```powershell
git rev-parse HEAD
git rev-parse origin/main
git diff --stat HEAD origin/main
git log --oneline --decorate -6
```

기대 결과:

- HEAD와 `origin/main` hash 동일
- `git diff --stat HEAD origin/main` 출력 없음
- 이 문서와 `3f0e142` 1단계, `5aa7fed` 2단계 구현 커밋이 history에 존재

## 4.5 Python 환경이 없는 경우

Python 3.11 또는 3.12를 사용한다.

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e '.\simulation\path_planning_lab[dev]'
```

1·2단계 동적 영향권 시험:

```powershell
.\.venv\Scripts\python.exe -m pytest `
  -c simulation\path_planning_lab\pyproject.toml `
  simulation\path_planning_lab\tests\test_dynamic_actor.py `
  simulation\path_planning_lab\tests\test_dynamic_simulation.py `
  simulation\path_planning_lab\tests\test_dynamic_observation.py `
  simulation\path_planning_lab\tests\test_dynamic_prediction.py `
  -q
```

기대 결과:

```text
53 passed
```

전체 시험은 기존 통합 runner 때문에 약 4분 이상 걸릴 수 있다.

---

# 5. 앞으로 할 일

## 5.1 다음 후보 작업: 3단계

기준 명세:

- [3단계 안전 게이트, 권한과 시간 계약](docs/research/dynamic-actor-experiment/03-safety-authority-and-timing.md)

목표:

```text
검증된 fresh observation snapshot과 Actor tube
→ controller 비종속 online command filter
→ 제한 감속·보호정지·실제 정지 확인
→ stop_epoch·재출발 권한·deadline 처리
```

3단계는 별도 사용자 지시 뒤 시작한다. 2단계 코드를 다시 구현하거나 PP·DWA closed
loop, 200 Hz evaluator, corpus·통계 작업을 함께 시작하지 않는다.

명세상 수정 후보:

```text
src/hospital_path_lab/dynamic_contracts.py
src/hospital_path_lab/dynamic_safety.py
src/hospital_path_lab/safety.py
src/hospital_path_lab/collision.py
tests/test_dynamic_safety.py
tests/test_dynamic_authority.py
tests/test_dynamic_timing.py
```

시작 전 확인할 경계:

- controller에 Actor ground truth를 직접 전달하지 않는다.
- 기존 정적 `SafetyGate` API를 깨지 않는다.
- 2단계 validator의 fresh snapshot과 공통 tube API를 재사용한다.
- 실제 사람·실차 안전성 또는 인증을 주장하지 않는다.
- 제품 알고리즘 채택, `G1~G5`, 경로 분석 7단계를 수행하지 않는다.

## 5.2 이후 순서

4. PP·DWA 동적 closed-loop 통합
5. 200Hz evaluator·golden/development/fault corpus
6. manifest 동결·hidden 30개·통계·최종 연구 판정

hidden은 1~5단계 구현과 development 수정이 끝난 뒤에만 생성한다. hidden을 본 뒤 코드를
수정하면 기존 hidden은 regression으로 전환하고 새 hidden을 만들어야 한다.

---

# 6. 브랜치 운영 원칙

- GitHub 기본 브랜치와 프로젝트 기준선은 `main`이다.
- 작고 직렬적인 완성 단위는 `main`에서 시작할 수 있다.
- 병렬 작업, 위험한 실험, 장기 변경은 `codex/<작업명>` 브랜치를 사용한다.
- 기능 브랜치를 사용해도 검증 완료된 작업은 `main`에 병합하고 원격 `main`을 갱신한다.
- 병합 전 `git fetch`, 작업트리 상태, ancestor·충돌 여부를 확인한다.
- 사용자 파일을 보존하지 않은 `reset --hard`, 강제 push, 무단 삭제는 하지 않는다.
- 집 PC 동기화 명령은 사용자가 요청할 때 제공한다.

---

# 7. 새 Codex 대화 시작 프롬프트

아래 내용은 새 Codex 대화에서 저장소 상태를 안전하게 인수하는 프롬프트다. 3단계 구현은
별도 지시로 범위를 확정한 뒤 시작한다.

```text
다음 GitHub 저장소의 작업을 이어서 진행한다.

저장소:
https://github.com/shotgun1107/hospital-autonomous-wheelchair.git

기준 브랜치:
main

현재 상태:
- GitHub 기본 브랜치는 main이다.
- 동적 원형 Actor 회피 비교실험의 1·2단계 구현은 완료됐다.
- 1단계 기능 커밋은 3f0e1425056514ad0bba77c9102f11afcb406554다.
- 2단계 기능 커밋은 5aa7fed5c7e4b3243a9e87db9688b563db183237다.
- seed 기반 원형 Actor·20Hz trace와 10Hz 열화 관측·source validation·공통
  Actor prediction tube가 구현됐다.
- 관측·예측 전용시험은 43 passed, 동적 영향권은 53 passed,
  전체 회귀시험은 197 passed였다.
- PP·DWA 동적 closed loop, 동적 safety gate, stop_epoch와 200Hz evaluator는 미구현이다.
- 제품 알고리즘 채택, G1~G5 결정, 경로 분석 7단계는 수행하지 않는다.

먼저 다음 문서를 순서대로 완전히 읽어라.
1. AGENTS.md
2. 인수인계.md
3. docs/research/dynamic-person-avoidance-experiment-plan-2026-08-10.md
4. docs/research/dynamic-actor-experiment/README.md
5. docs/research/dynamic-actor-experiment/02-observation-and-prediction.md
6. docs/research/dynamic-actor-experiment/03-safety-authority-and-timing.md
7. simulation/path_planning_lab/README.md
8. simulation/path_planning_lab/TRACEABILITY.md

먼저 로컬 상태를 확인한다.
- git status -sb
- git status --short
- git remote -v
- git fetch origin --prune
- git switch main
- git pull --ff-only origin main
- git rev-parse HEAD
- git rev-parse origin/main

로컬 변경이 있으면 삭제하거나 덮어쓰지 말고 backup 브랜치와 commit으로 보존한다.
main과 origin/main이 다르거나 fast-forward가 실패하면 임의로 reset하지 말고 차이를
보고한다.

이번 첫 작업은 동기화·문서·커밋·시험 상태를 확인하고 2단계가 실제로 존재함을
인수하는 것뿐이다. 1·2단계를 다시 구현하거나 3단계 이후 코드를 작성하지 않는다.
확인 결과를 간결하게 보고한 뒤 사용자의 다음 범위 지시를 기다린다.
```

---

# 8. 다음 담당자가 기억할 핵심

```text
정본은 GitHub main이다.
1·2단계는 끝났다. 다시 만들지 않는다.
다음 후보는 3단계 안전·권한·시간 계약이며 별도 범위 지시 뒤 시작한다.
ground truth를 controller에 누출하지 않는다.
hidden은 아직 열지 않는다.
완료 단위는 시험·문서와 함께 main에 반영한다.
```
