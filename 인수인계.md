# 병원 자율주행 휠체어 프로젝트 인수인계

## 1. 문서 목적

이 문서는 2026-08-11 회사 PC에서 수행한 동적 원형 Actor 비교실험 작업을 GitHub `main`을
통해 집 PC로 넘기기 위한 인수인계다. 다음 내용을 한곳에 기록한다.

- 과거: 동기화와 실험 과정에서 발생한 문제
- 현재: 구현·검증·실험 결과와 증거 범위
- 미래: 집 PC에서 안전하게 동기화하고 이어서 할 작업
- 시작 프롬프트: 같은 Codex 작업에 붙여 넣을 인수 지시

이 문서는 제품 알고리즘 채택 문서가 아니다. 모든 결과는 가상 차체와 open-loop 원형
Actor를 사용한 Python `simulation_only` 연구 결과다. 실제 사람 탑승 안전성, 의료기기
인증, `G1~G5`, 경로 분석 7단계 또는 제품 알고리즘을 결정하지 않았다.

### 1.0.1 회사 PC C++ timing 후속 — 2026-08-12

기존 Python+NumPy DWA의 `100/500` miss를 해결하기 위해 후보·rollout·terminal stopping·
충돌·비용 반복 계산을 선택적 C++20 코어로 옮겼다. 217개 후보, 후보당 41 pose, 비용·
tie-break와 외부 shared safety gate는 유지했다. 공개 대표 98 snapshot에서 Python과 C++의
controller·diagnostic digest 불일치는 `0/98`, 동적·DWA 영향권 회귀는 `212 passed`였다.

첫 공식 5-case×100은 코너 geometry 병목으로 C++ DWA miss `1/500`이어서 실패로 보존했다.
증명 가능한 distance-field lower bound로 불필요한 exact cell scan만 생략한 뒤 같은 조건을
한 번 재실행했고, PP와 C++ DWA 모두 miss `0/500`을 기록했다. C++ DWA는 p50
`3.770 ms`, p95 `15.459 ms`, p99 `16.714 ms`, 최대 `35.576 ms`였다. 이것은 독립 timing
하위 자격 통과일 뿐이며 expanded public, receipt와 새 hidden은 아직 실행하지 않았다.
따라서 아래 Python-only 설명은 2026-08-11 당시의 역사적 상태로 읽는다.

### 1.1 집 PC v6 후속 작업 — 2026-08-11

집 PC는 회사 정본 `main@fe383b010164e0fc35c932460a6c3200f80d9fd7`, tree
`5fb734678b437cc773343058484abb55b472fa98`로 fast-forward 동기화한 뒤
[v6 보정·재자격 명세](docs/research/dynamic-actor-experiment/v6-correction-and-requalification.md)를
먼저 작성하고 보정 구현을 시작했다.

현재 반영 범위는 다음과 같다.

- legacy-v1 공개 36개와 exact hash를 그대로 유지하고 v6 공개 13개를 별도 생성한다.
- 같은 방향, 정면, 대각선, 코너·교차로, 세로 90° pair와 여러 Actor를 포함한다.
- source·map·Actor ID를 label-free opaque 값으로 만들고 semantic-world/oracle hash를
  분리한다.
- feasible label은 20 Hz 차체 적분·가감속 제한과 200 Hz static·forbidden·Normal/Stress
  tube를 검사하는 controller 비종속 witness가 있어야 한다.
- evaluator는 simulator와 같은 Euler tick 계약을 200 Hz로 재적분하고, hazard별 별도
  stop epoch·허가된 중간 재출발과 `이탈 → 추월 → 재합류` 순서를 검사한다.
- DWA 후보 수·rollout·비용·tie-break·외부 shared gate는 유지하면서 후보 탈락 진단과
  step-local 정적·금지구역 기하 재사용을 추가한다.
- runner·CLI는 public-only 재자격만 노출한다. canonical full 범위가 아닌 축소·주입
  실행은 report-only이며 receipt를 만들지 못하고, hidden 실행 경로는 제거했다.
- 전체 결과 조합·90° rigid 결과·signature hash·source freeze를 receipt 직전까지
  fail-closed로 검증한다.

동작보존 최적화 전후 공개 대표 snapshot 98개의 semantic digest는 일치했다. 전체
pytest는 동적 `187`개와 기존 실험실 `148`개, 합계 `335 passed`다. 그러나 독립
5-case×100 측정에서 PP miss는 `0/500`, DWA miss는 `100/500`
(`p50 27.506 ms`, `p95 58.033 ms`, 최대 `75.957 ms`)였다.
따라서 expanded public의 정식 full qualification과 전 case 50 ms 통과를 완료하지 않았다.
회사 `final-v4` ignored output도 집 PC에 없으므로 전체 artifact 동작보존을 통과했다고
주장하지 않는다. 새 hidden은 생성·소비하지 않았고 v6 승격·제품 알고리즘 결론도 없다.

현재 DWA 핵심은 Python 3.12와 NumPy로 작성됐다. 후보 217개와 후보당 41 pose의 반복 검사,
rollout, 충돌·거리 계산을 Python에서 수행하며 Cython·Numba·C·C++ 구현은 없다. 이번 결과는
`DWA 알고리즘 자체를 폐기`한다는 뜻이 아니라 `현재 Python 구현은 20 Hz의 엄격한 50 ms
무실패 기준에 아직 사용할 수 없다`는 뜻이다. 다음 구현 방식은 확정하지 않았으며, 선택지는
Python 실험실을 유지하면서 느린 계산만 Cython으로 옮겨 먼저 재시험하거나 DWA 계산 코어를
C++로 옮기는 것이다.

---

## 2. 정본과 브랜치 원칙

| 항목 | 값 |
|---|---|
| 저장소 | `https://github.com/shotgun1107/hospital-autonomous-wheelchair.git` |
| 기본·정본 브랜치 | `main` |
| 회사 작업 브랜치 | `codex/dynamic-stage6-handoff` |
| 동기화 전 `main` | `b70f7e392e8c303861495e8f1dfaf8060c74c3d2` |
| 1단계 구현 | `3f0e1425056514ad0bba77c9102f11afcb406554` |
| 2단계 구현 | `5aa7fed5c7e4b3243a9e87db9688b563db183237` |
| 3~6단계 구현 기준 | `f89713c72c130c89b6d095a77ef90540aa5768f5` |
| v6 보정 구현 | `90778644ca1f9de78a6f36244db2170185d0f513` |
| v6 보정 구현 tree | `29569488fc348be226275143ea04532867399753` |

작업 브랜치는 검증과 변경 보존에 사용하지만, 완료 작업은 `main`에 fast-forward 병합하고
`origin/main`까지 갱신해야 집 PC가 정본을 받을 수 있다. 강제 push, `reset --hard`, 사용자
파일 삭제와 대용량 `outputs/` 커밋은 금지한다.

---

## 3. 과거 — 어떤 문제가 있었고 어떻게 처리했는가

### 3.1 초기 브랜치 동기화 문제

초기 작업은 기능 브랜치에만 존재하고 GitHub 기본 브랜치인 `main`에 즉시 반영되지 않은
적이 있었다. 데이터 손실은 없었지만 집 PC가 `main`만 당기면 완료 작업을 받지 못하는
상태였다.

해결 원칙은 다음과 같이 고정했다.

1. GitHub 기본 브랜치는 `main`으로 유지한다.
2. 큰 작업은 `codex/<작업명>` 브랜치에서 수행한다.
3. 검증이 끝난 완료 작업은 fast-forward 방식으로 `main`에 포함한다.
4. `main`, `origin/main`, GitHub 원격 HEAD의 일치를 확인한다.

### 3.2 GitHub CLI 인증과 Git 자격증명의 차이

회사 PC의 `gh auth status`는 만료된 GitHub CLI 토큰을 보고했다. 그러나 Git 자체는 Windows
Git Credential Manager의 기존 자격증명으로 원격 저장소에 접근할 수 있었다. 따라서 이번
동기화는 `gh` 토큰에 의존하지 않고 Git Credential Manager를 사용한다. 이 우회는 인증
검사를 생략한 것이 아니라 Git 원격 접근을 `git ls-remote`로 먼저 검증한 뒤 사용하는
방식이다.

### 3.3 생성 결과물은 Git으로 전달되지 않음

다음 경로는 `.gitignore` 대상이다.

```text
simulation/path_planning_lab/outputs/
```

`final-v1`~`final-v4`의 JSON·PNG는 회사 PC에 보존되지만 GitHub와 집 PC로 자동 전달되지
않는다. 코드·고정 corpus·문서·시험은 Git으로 전달하며, 필요하면 집 PC에서 같은 seed로
결과를 다시 생성한다. 대용량 결과물을 억지로 Git에 넣지 않는다.

### 3.4 실험 실행 중 발견한 문제

#### 평가기와 시뮬레이터의 회전 계산 불일치

초기 evaluator는 정확한 원호를 사용하고 simulator endpoint는 Euler 적분을 사용했다.
정상 회전이 provenance 오류로 잘못 판정됐다. endpoint provenance는 simulator와 같은 Euler
방식으로 맞추고, 실제 충돌 여유 평가는 기존 200 Hz 세부 적분을 유지했다. `final-v1`은
이 결함이 포함된 실패 증거로 보존했다.

#### 실행시간 과다와 CPU 저활용

처음에는 독립 episode도 직렬 실행하여 시간이 과도하게 걸렸다. 이후 다음 구조로 바꿨다.

```text
episode × observation_profile
→ process 기반 병렬 실행
→ 같은 worker에서 같은 입력으로 PP 후 DWA 실행
→ 부모가 결과를 결정론적 순서로 재정렬

50 ms wall-clock qualification
→ worker pool 종료 후 CPU 간섭 없이 단독 직렬 실행
```

28 logical CPU인 회사 PC에서 기본 6 worker는 약 20%만 사용했다. 최종 실행은 명시적으로
14 worker를 사용하여 결과 계산에 약 50%의 논리 CPU를 배정했다. wall-clock 성능 측정은
공정성을 위해 병렬화하지 않았다.

#### 공개 기능시험 게이트 누락

가장 큰 실험 기획 오류다. 공개 development에서 DWA가 실제 우회하지 못했는데도 기존
runner는 공통 hard safety만 통과하면 hidden을 실행했다. 그 결과 기능 자격과 50 ms 자격이
없는 구현으로 hidden을 소비했다.

올바른 순서는 다음과 같다.

```text
공개 hard safety·fault 자격
→ 공개 기능시험에서 실제 detour·rejoin 확인
→ 50 ms 직렬 qualification 통과
→ 그다음 새 hidden 생성·실행
```

기존 `final-v4` hidden은 이미 확인했으므로 새 최종판정에 재사용하지 않고 regression으로만
사용한다.

#### 시나리오 방향 부족

현재 full corpus는 직선 복도에서 휠체어가 주로 `+x` 방향으로 움직이고, Actor는 정지하거나
주로 횡단하는 구성이 중심이다. 가로·세로 크기를 가진 회전 직사각형 footprint 자체는
구현했지만 방향별 주행 환경을 충분히 시험하지 않았다.

아직 별도 검증이 필요한 시나리오는 다음과 같다.

- 같은 방향 사람
- 정면 접근 사람
- 대각선 횡단
- 코너와 교차로
- 세로 방향 경로
- 여러 사람

---

## 4. 현재 — 회사 PC에서 완료한 것

### 4.1 1~3단계

1단계의 결정론적 Actor/20 Hz trace, 2단계의 10 Hz 관측·noise·dropout·source validation·
Actor prediction tube는 기존 기준선을 유지한다.

3단계에서 다음을 구현했다.

- 공통 동적 safety gate
- stale·invalid·late 결과의 제한 감속과 폐기
- 선속도·각속도 정지 과정
- static grid·금지구역·Actor tube swept 검사
- 실제 정지 확인과 `stop_epoch` 1회 증가
- 이전 이동 허가 무효화와 현재 epoch 재승인
- 위험 해소만으로 자동 재출발하지 않는 권한 계약

### 4.2 4단계 PP·DWA closed loop

- PP와 사용자 정의 DWA가 같은 snapshot, Actor 관측과 safety gate를 사용한다.
- 두 controller의 자유주행 목표속도는 `0.20 m/s`로 맞췄다.
- DWA는 최대 `217`개 `(v, w)` 후보, 후보당 40개 rollout 구간과 terminal stopping을
  검사한다.
- controller 요청, gate 적용 명령, override와 hold 원인을 분리해 기록한다.

### 4.3 5단계 evaluator와 corpus

- controller 입력과 분리된 실제 Actor ground truth evaluator
- 200 Hz swept clearance 판정
- golden 6개, development 30개
- observation 13종, authority 7종, deadline 5종의 contract-fault catalog
- 충돌, `0.08 m` clearance, 금지구역, stale/invalid 추진, 무단 재개, late 적용과
  provenance 검증

### 4.4 6단계 runner와 결과물

- PP/DWA paired whole-episode runner
- manifest/source hash와 hidden seed commitment/consumption receipt
- process 기반 병렬 결과 계산과 별도 직렬 timing qualification
- paired 통계, Pareto, 승격 조건 10개
- JSON·Markdown·PNG·regression candidate 출력
- hidden 확인 뒤 코드를 바꾸면 기존 hidden을 regression으로 전환하는 수명주기

최종 보존 실행:

```text
simulation/path_planning_lab/outputs/dynamic-experiment-20260811-final-v4
```

회사 PC 로컬 기준 산출물은 198개, 약 5.94 MiB이며 PNG 120개와 regression candidate
67개를 포함한다. 이 디렉터리는 Git에 포함하지 않는다.

### 4.5 `final-v4` 핵심 결과

- worker: 14 process, 결과 계산만 병렬
- 공개 run: 144
- hidden run: 120
- 전체 hard safety: `264/264` 통과
- contract fault: `25/25` 통과
- hidden Normal 기능: PP `27/30`, DWA `16/30`
- hidden Stress 기능: PP `5/30`, DWA `5/30`
- DWA feasible detour/rejoin: `0%`
- 직렬 50 ms qualification: PP miss `0/400`, DWA miss `324/400`
- DWA p50 약 `65.6 ms`, p95 약 `81.6 ms`
- 승격 조건 3·4·5·10 미달
- DWA 승격 실패, `PP + shared gate`를 임시 연구 기준선으로 유지

정확한 해석은 다음과 같다.

> DWA 알고리즘 자체가 PP보다 나쁘다고 증명한 것이 아니다. 현재 Python DWA 구현은
> 계산시간과 실질적 우회 기능 자격을 충족하지 못했다. 공통 gate가 위험 명령을 많이
> 막았기 때문에 hard safety 통과만으로 DWA가 잘 동작했다고 볼 수도 없다.

`PP + shared gate` 역시 제품 채택 결론이 아니라 현재 동작이 확인된 임시 연구 기준선이다.

### 4.6 검증 상태

아래는 집 PC v6 보정 전 회사 PC에서 확인했던 역사적 기준이다. 현재 기준은 1.1절의
`335 passed`와 별도 50 ms 측정 결과다.

```text
전체 pytest: 274 passed
Ruff: passed
git diff --check: passed
```

집 PC에서는 동기화 후 같은 환경에서 다시 확인한다. 실험 full run은 약 18분 30초가
걸렸으며, 일반 회귀시험과 다르므로 코드 인수 직후 무조건 재실행하지 않는다.

---

## 5. 결론 — 무엇을 유지하고 무엇을 다시 하는가

v6 보정 구현과 회귀는 완료했다. **50 ms 무실패 자격만 아직 통과하지 못했다.**

| 단계 | 처리 |
|---|---|
| 1. 동적 시뮬레이션 기반 | 유지, 전체 회귀 통과 |
| 2. 관측·Actor 예측 | 유지, 여러 Actor 회귀 보강 완료 |
| 3. safety gate·권한·시간 | 유지, 연속성·epoch 검증 보강 완료 |
| 4. PP·DWA 통합 | 진단·동작보존형 최적화 완료, 50 ms gate 미달 |
| 5. evaluator·corpus | 방향·공간·여러 Actor 공개 v6 확장 완료 |
| 6. runner·hidden | public-only gate 완료, receipt 미발급·hidden 차단 유지 |

`final-v4`는 삭제하지 않고 **현재 실패 구현의 회귀자료**로 보존한다. 새 코드가 기존
안전 계약을 깨지 않는지 확인하는 데 사용할 수 있지만, 새로운 최종 알고리즘 비교의
hidden 증거로 사용하지 않는다.

---

## 6. 집 PC에서 동기화하는 방법

### 6.1 먼저 로컬 상태 확인

```powershell
Set-Location '<집 PC 저장소 경로>'
Get-Content -LiteralPath AGENTS.md -Raw
git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes
```

집 로컬 변경을 삭제하거나 덮어쓰지 않는다. `.env`, token, key, credential이 보이면
stage하지 말고 중단한다. 민감하지 않은 미보존 변경이 있다면 원격에 push하지 않는 backup
브랜치와 commit으로 먼저 보존한다.

```powershell
git switch -c backup/home-before-dynamic-v6-sync-20260811
git add -A
git commit -m "backup home work before dynamic v6 sync"
```

### 6.2 정본 `main` 동기화

작업트리가 깨끗하거나 backup이 완료된 뒤 실행한다.

```powershell
git fetch origin --prune
git switch main
git pull --ff-only origin main
git status -sb
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git merge-base --is-ancestor 90778644ca1f9de78a6f36244db2170185d0f513 HEAD
```

fast-forward가 실패하거나 `main`과 `origin/main`이 갈라졌다면 reset, rebase 또는 임의 merge를
하지 말고 양쪽 commit과 영향을 받는 파일을 보고한다.

### 6.3 코드 인수 검증

Python 환경이 이미 있으면 다음을 실행한다.

```powershell
.\.venv\Scripts\python.exe -m pytest `
  -c simulation\path_planning_lab\pyproject.toml `
  simulation\path_planning_lab\tests `
  -q

.\.venv\Scripts\python.exe -m ruff check `
  simulation\path_planning_lab\src `
  simulation\path_planning_lab\tests
```

의존성이 없다면 Python 3.12 환경을 준비한 뒤 editable dev dependency를 설치한다.

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e '.\simulation\path_planning_lab[dev]'
```

---

## 7. 다음 PC에서 이어서 할 작업

### 7.1 완료되어 다시 구현하지 않는 범위

v6 보정 구현은 `90778644ca1f9de78a6f36244db2170185d0f513`에 고정했다.

- legacy-v1 공개 36개와 v6 공개 13개 corpus, 기능 oracle과 rigid 90° pair
- DWA 후보 탈락 진단, 동작보존형 static·forbidden·Actor 계산 재사용
- simulator와 evaluator의 시간·상태 연속성 및 stop epoch 검증
- episode×profile 단위 process 실행과 같은 worker의 PP→DWA paired 실행
- canonical full 공개 실행만 receipt를 만들 수 있는 public-only runner·CLI
- source freeze, 전체 결과 조합, rigid signature와 receipt의 fail-closed 검증
- 동적 187개와 기존 실험실 148개, 총 335개 회귀시험

이 범위를 다시 구현하거나 기존 `final-v4` hidden을 최종평가에 재사용하지 않는다.

### 7.2 현재 차단 조건

동작보존 검증과 회귀시험은 통과했지만 50 ms 무실패 자격은 통과하지 못했다.

```text
standalone qualification: 5 cases × 100 repeats
PP: 0 / 500 deadline misses
DWA: 100 / 500 deadline misses
DWA p50: 27.506 ms
DWA p95: 58.033 ms
DWA max: 75.957 ms
snapshot set: 77f876702b4db90ea729740b3eb96187ae14fba26f045c2ea63c96dd678bb8af
```

따라서 다음 항목은 아직 완료가 아니다.

- expanded public canonical full qualification
- 공개 자격 receipt 발급
- 새 hidden 생성·소비
- DWA 승격 또는 제품 알고리즘 채택

### 7.3 다음 작업의 허용 범위

새 PC에서는 먼저 동기화와 상태 인수만 보고한다. 현재 DWA는 Python+NumPy 구현이며
Cython·Numba·C·C++ 코어는 없다. 사용자가 후속 구현 방식을 선택하기 전에는 언어 전환이나
추가 최적화를 시작하지 않는다.

후속 선택지는 다음 두 개다.

1. Python 실험환경과 API를 유지하고 느린 DWA·충돌 반복 계산만 Cython으로 옮겨 재시험한다.
2. 장기적인 로봇 실행을 목표로 DWA·충돌 계산 코어를 C++로 옮기고 Python 실험실과 비교한다.

Cython을 먼저 시험하더라도 500회 무실패와 동일 동작을 만족하지 못하면 C++ 후보로 다시
평가한다. C를 별도 우선 후보로 채택하지 않는다. 어느 방식도 아직 승인·구현된 결정이 아니다.

다음 조건은 변경하지 않는다.

- 후보 수 217개, 후보당 41 pose, 2 s rollout과 50 ms 간격
- terminal stopping과 5 ms shared safety sweep
- Actor tube, static·forbidden clearance, 비용, rank와 tie-break
- 외부 shared safety gate의 독립 권한

샘플 수·안전 여유·Actor tube·비용을 자격 결과에 맞춰 완화하거나 persistent 결과 cache로
qualification을 우회해서는 안 된다. 변경 전후 controller·diagnostic semantic digest와 전체
회귀를 다시 비교하고, worker pool이 종료된 부모 process에서 독립 직렬 자격을 측정한다.

### 7.4 hidden과 증거 경계

공개 contract fault·hard safety·기능 oracle·50 ms 무실패 자격이 canonical full 범위에서 모두
통과하고 유효한 receipt가 발급되기 전에는 새 hidden을 만들거나 열지 않는다. 집 PC에는 회사
`final-v4` ignored output이 없으므로 전체 artifact 동작보존을 주장하지 않는다. 모든 결과는
Python `simulation_only` 연구 증거이며 실제 사람 탑승 안전성, `G1~G5`, 경로 분석 7단계와
제품 알고리즘 결론으로 확대하지 않는다.

---

## 8. 같은 Codex 작업에 붙여 넣을 시작 프롬프트

아래 프롬프트는 새 PC에서 Git 정본을 안전하게 동기화하고 현재 v6 차단 상태를 인수하도록
작성했다. 동기화 직후 임의로 추가 구현하지 않는다.

```text
hospital-autonomous-wheelchair 프로젝트의 작업을 새 PC에서 이어서 진행한다.

정본:
- repository: https://github.com/shotgun1107/hospital-autonomous-wheelchair.git
- default branch: main
- required v6 implementation commit: 90778644ca1f9de78a6f36244db2170185d0f513
- required v6 implementation tree: 29569488fc348be226275143ea04532867399753

먼저 현재 PC의 AGENTS.md와 상위 AGENTS.md를 완전히 읽고 따른다.
그다음 구현하지 말고 로컬 상태를 먼저 확인한다.

git status -sb
git status --short
git remote -v
git branch -vv
git stash list
git log --oneline --branches --not --remotes

로컬 변경을 삭제하거나 덮어쓰지 마라. 민감 파일이 없고 미보존 변경이 있다면 원격에
push하지 않는 backup 브랜치와 backup commit으로 보존한다. reset --hard, clean, force push,
무단 stash와 rebase는 금지한다.

보존 후 또는 처음부터 깨끗하면 다음을 수행한다.

git fetch origin --prune
git switch main
git pull --ff-only origin main
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-parse origin/main
git cat-file -t 90778644ca1f9de78a6f36244db2170185d0f513
git merge-base --is-ancestor 90778644ca1f9de78a6f36244db2170185d0f513 HEAD
git rev-parse '90778644ca1f9de78a6f36244db2170185d0f513^{tree}'
git status --porcelain=v1

fast-forward가 실패하거나 main과 origin/main이 갈라지면 임의로 해결하지 말고 양쪽 commit,
backup 브랜치와 영향을 받는 파일을 보고한다.

동기화 뒤 다음 문서를 순서대로 완전히 읽는다.
1. AGENTS.md
2. 인수인계.md
3. docs/research/dynamic-person-avoidance-experiment-plan-2026-08-10.md
4. docs/research/dynamic-actor-experiment/README.md
5. docs/research/dynamic-actor-experiment/v6-correction-and-requalification.md
6. docs/research/dynamic-actor-experiment/03-safety-authority-and-timing.md
7. docs/research/dynamic-actor-experiment/04-controller-integration.md
8. docs/research/dynamic-actor-experiment/05-evaluator-and-corpus.md
9. docs/research/dynamic-actor-experiment/06-runner-hidden-and-reporting.md
10. simulation/path_planning_lab/README.md
11. simulation/path_planning_lab/TRACEABILITY.md

인수할 핵심 상태:
- v6 보정 구현은 완료됐으므로 다시 구현하지 않는다.
- legacy-v1 공개 36개와 v6 공개 13개, 기능 oracle, 90° rigid pair가 구현됐다.
- public-only runner는 canonical full 범위에서만 receipt를 봉인하며 hidden 실행 경로는 차단됐다.
- 동적 187개와 기존 실험실 148개, 총 335개 회귀시험이 통과했다.
- standalone 5-case×100에서 PP는 0/500 miss, DWA는 100/500 miss였다.
- DWA 수치는 p50 27.506 ms, p95 58.033 ms, max 75.957 ms다.
- 따라서 expanded public full qualification과 공개 receipt는 아직 없고 새 hidden도 생성하지 않았다.
- 이 결과는 현재 Python 구현의 50 ms 자격 미달 증거이며 DWA 알고리즘이나 제품 채택 판정이 아니다.
- 회사 final-v4 ignored output은 이 PC에 없으면 전체 artifact equivalence를 주장하지 않는다.
- G1~G5, 경로 분석 7단계, 실제 사람 탑승 안전성은 이 작업 범위가 아니다.

첫 응답에서는 동기화 전후 상태, backup 여부, HEAD/tree, origin/main 일치, 작업트리 clean,
v6 implementation ancestor/tree 여부와 현재 50 ms gate 상태만 보고한다. 구현·full public
qualification·receipt·hidden·commit·push는 시작하지 말고 다음 지시를 기다린다.

현재 DWA 핵심은 Python+NumPy이며 Cython·Numba·C·C++ 코어는 없다. 첫 보고 뒤 사용자가
`Cython으로 느린 계산만 먼저 옮길지` 또는 `C++ 코어로 바로 옮길지` 선택할 때까지 구현하지
않는다. 어느 쪽을 선택해도 Python 실험환경과 기존 결과를 비교 기준으로 유지한다.

후속 구현에서는 후보 수 217, 41 pose, 2 s/50 ms rollout, terminal stopping, 5 ms safety
sweep, Actor tube, clearance, 비용·rank·tie-break와 외부 shared gate를 완화하거나 바꾸지
않는다. 같은 공개 입력에서 경로·정지·진단 결과가 같음을 먼저 확인한 뒤 500회 50 ms
무실패 시험을 수행한다.
```

---

## 9. 한 줄 인계

```text
v6 공개 corpus·oracle·runner와 Python DWA 최적화는 구현·회귀 완료됐지만 DWA가 독립 50 ms
자격에서 100/500 miss로 미달했다. 다음 PC는 동기화 후 Python+NumPy 구현이라는 현재 상태를
보고하고, 사용자가 Cython 또는 C++ 방향을 선택하기 전에는 추가 구현이나 full
public·receipt·hidden을 시작하지 않는다.
```
