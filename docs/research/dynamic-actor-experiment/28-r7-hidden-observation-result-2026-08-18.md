# R7 후속 비공개 관측 시험 결과

- 판정: `FAIL`
- 실행일: `2026-08-18`
- 실행 commit: `f4da7e08fffe05d3ef0317f17332eb6195a2ee8b`
- 실행 tree: `683f59f7f732a8d38a3a253c92eac7cdf2782dc7`
- 전체 회귀: `958 passed`
- 비공개 값 지문: `eb8c18f77bd111059af404f4a22f4dd512c67fd87b7c1df183bb6a8d75806aed`
- 결과 집합 지문: `81e52fd47d7eb5241bef0b28e268f4ed51a8275ebedf1cb603effac6eff0c661`
- 증거 ZIP: `simulation/path_planning_lab/outputs/r7-hidden-observation-fail-evidence-20260818-f4da7e0.zip`
- ZIP 크기: `7,007 bytes`
- ZIP SHA-256: `23c91b7df9ee023d4d3bd73378fd49a415e7becc8ac89b75092ee3e4016a6ac6`

## 1. 한 줄 결론

새 관측 잡음과 누락 순서 20건에서는 충돌하거나 벽에 닿지는 않았지만, Normal 4건이
목적지 완료에 실패하고 Stress 1건이 잠깐 출발했다. 따라서 현재 연구 기준선은 새 관측
순서에서도 안정적으로 동작한다고 볼 수 없다.

## 2. 실행 방법

기준·판정·실행기를 먼저 commit과 push한 뒤 OS 난수로 새로운 값을 한 번 만들었다. 같은
replica·side의 Normal과 Stress는 같은 기본 값을 공유한다. 20건은 10개 process로 병렬
실행했으며 완료 순서와 무관하게 고정 순서로 다시 합쳤다.

속도 측정은 이 시험의 합격 기준이 아니다. R7의 기존 50ms 자격 `0/500`을 재사용했고
500회 시간시험은 다시 돌리지 않았다.

## 3. 결과

| 항목 | 결과 | 요구 | 판정 |
|---|---:|---:|---|
| 전체 case | `20/20` 완주 | `20/20` | 통과 |
| Normal 목적지 완료 | `6/10` | `10/10` | 실패 |
| Stress 최종 보수 정지 | `10/10` | `10/10` | 결과만 통과 |
| Stress 무출발 | `9/10` | `10/10` | 실패 |
| 코드·계약 중단 | `2` | `0` | 실패 |
| 충돌·금지구역·0.08m 위반 | `0` | `0` | 통과 |

전체 최소 실제 Actor 여유는 약 `0.3394m`, 최소 정적 장애물 여유는 약 `0.3783m`였다.
따라서 이번 실패는 물리 충돌이 아니라 완료 신뢰성과 상태 관리 문제다.

## 4. 실패한 다섯 건

### `hidden-01-right-normal`

- Actor 통과와 원 경로 복귀 단계까지 진행했다.
- 정지·재출발을 반복한 뒤 최종 `HOLDING`으로 끝나 목적지에 도착하지 못했다.
- 코드 예외와 물리 안전 위반은 없었다.

### `hidden-02-left-normal`

- tick `259`에서 `R5-B continuation authorization is incomplete` 예외가 발생했다.
- 최종 상태는 `BRAKING`이었다.
- 관측 누락 뒤 이어서 이동할 때 필요한 허가 상태가 완성되지 않은 채 controller가 호출된
  코드 경계 문제다.

### `hidden-02-right-normal`

- tick `503`에서 `active_section_not_in_current_window`로 중단됐다.
- 최종 상태는 `BRAKING`이었다.
- 현재 실행 중인 경로 구간과 controller가 들고 있는 경로 구간이 어긋난 상태 관리 문제다.

### `hidden-03-left-normal`

- 실제 이동과 여러 차례 재출발은 있었지만 Actor 통과 증거까지 만들지 못했다.
- 최종 `HOLDING`으로 종료했고 코드 예외·물리 안전 위반은 없었다.

### `hidden-03-left-stress`

- 최종 결과는 `conservative_hold`였지만 tick `531`에 재출발 허가가 생기고 tick `532`부터
  실제로 움직였다.
- controller가 4회 호출되고 gate가 2회 개입한 뒤 다시 멈췄다.
- Stress는 처음부터 끝까지 움직이지 않아야 한다는 사전 기준을 위반했다.

## 5. 의미

확인된 것은 다음이다.

- 공통 안전장치는 이 20건에서 충돌과 금지구역 진입을 막았다.
- 현재 경로 실행 코드는 관측 누락이 달라지면 재출발과 경로 구간 상태가 흔들린다.
- 공개 시험 통과만으로 새로운 관측 순서의 완료를 보장하지 못한다.

확인되지 않은 것은 다음이다.

- 새 지도와 새로운 사람 움직임에서의 성능
- 실제 카메라 관측에서의 성능
- 실물 휠체어와 실제 사람의 안전
- 제품 알고리즘 채택과 `G1~G5`

## 6. 다음 작업

1. 이 비공개 결과를 읽기 전용 회귀 자료로 보존한다.
2. `continuation authorization`과 `active section window` 두 코드 오류를 공개 재현 입력으로
   옮긴다.
3. Normal에서 반복 정지 뒤 완료하지 못한 두 건과 Stress에서 잠깐 출발한 한 건을 공개
   재현시험으로 만든다.
4. 수정은 새 공개 재현시험에서만 수행한다.
5. 이 비공개 값을 수정 후 최종 비공개 합격시험으로 재사용하지 않는다. 새 최종 비공개
   시험은 새 기준과 새 값으로 별도 승인 뒤 만든다.

이 결과는 simulation 연구의 실패 기록이며 제품 알고리즘이나 사람 탑승 안전 판정이 아니다.
